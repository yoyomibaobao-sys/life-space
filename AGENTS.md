# LifeSpace 现行开发总纲

本文件仍命名为 `AGENTS.md`，位于仓库根目录。用户所说的“总纲”或“纲领”均指本文件。

本次整理日期：2026-09-09。依据本轮已确认的修改，以及[《5、继续确认核对上线》](https://chatgpt.com/share/6aa0db8e-eb40-83ec-a131-51b654ece80c)中最后确认的决定；被后续讨论更正的早期提议不再作为执行依据。

文档职责约定：

| 文件 | 职责 |
| --- | --- |
| `AGENTS.md` | 现行产品与开发规则、本轮实现目标及数据保护边界 |
| `docs/product-roadmap.md` | 开发阶段、优先级和后续方向 |
| `docs/plans/regional-map.md`、`docs/plans/merchant-membership.md` | 地区地图、商社会员的专题计划 |
| `docs/public-launch-checklist.md` | 实际发布前的配置、测试与验收记录 |

规划文档按上述路径继续维护，不改名或合并成一个大文件。规划分支中的文档尚未出现在当前检出版本时，应先核对分支，不能据此认定计划未讨论过或已合并上线。

总纲中的“本轮目标”和“下一阶段”不表示功能已经实现、验证或发布。更新本文档不代表执行代码发布或生产数据库变更；执行状态以实际代码、测试和发布记录为准。

## 一、项目核心定位

本项目的主品牌是：有时。
产品对外总称是：LifeSpace·自然，中文解释为“有时自然生活空间”。
耕作 / 植物子页名称是：有时·耕作；英文参考名称是 LifeSpace for Cultivation。
产品副标题是：一个围绕耕作展开的生活空间。
经营、收款及门牌使用名称是：有时空间。

产品始终以耕作为主线：记录人与植物、土地及其周围自然生态长期共同变化的过程。

农法设施支撑耕作；虫鱼生态围绕水体、庭院和种植生态延伸；“其他”承载美食、自产利用等轻量延伸；集市与未来商社服务真实耕作及相关供需。各项扩展都应强化或支撑耕作，不能把主线改成泛生活平台、强社交社区或普通电商。

内容分类保留“种植｜农法设施｜虫鱼生态｜其他”。农法设施包含农法、设备、工具、工程与技能；虫鱼生态包含水草、水生生物、虫蝶、鸟类和庭院动物。APP 中分类按钮可简写为“农设／虫鱼”，网页端保留全称；简写不改变分类含义或数据标识。

品牌层级为：

* 主品牌：有时
* 产品总称：LifeSpace·自然 / 有时自然生活空间
* 耕作 / 植物子页：有时·耕作
* 产品解释：一个围绕耕作展开的生活空间
* 英文参考：LifeSpace for Cultivation
* 经营 / 收款名称：有时空间

以后不要把主标题写成“有时。耕作 生活空间”。
整站介绍、会员说明和公开元数据使用产品总称；耕作 / 植物子页使用“有时·耕作”。品牌文案调整不改变 Android 应用身份或用户数据归属。
“生活空间”作为产品解释和副标题使用，不强行挤进主标题。
“有时空间”用于经营收款方、运营物料和门牌，不替代 App、网站与服务条款中的产品名称。
品牌标记统一使用绿色圆角方形中的白色双叶幼芽；APP、网站、付款物料与门牌使用同一图形源。

以下是核心内容与长期能力方向。其中自动化、传感器、AI、机器人和生态模型属于后续规划，不是当前已提供的会员权益：

* 作物档案
* 地块 / 花盆 / 种植床 / 小院 / 阳台记录
* 时间线耕作记录
* 图片与媒体记录
* 播种、育苗、移栽、修剪、施肥、浇水、采收记录
* 病虫害、天气、土壤、光照、水分等观察
* 作物库
* 农法与种植方法
* 设施制作与工具记录
* 技术技能记录
* 耕作地图 / 地块平面图
* 本地气候
* 自动化灌溉
* 传感器数据
* AI 辅助照料
* 未来机器人照料植物
* 可计算的植物生态模型

其他内容，例如：

* 插花
* 写字
* 萌宠
* 健身
* 旅行
* 其他个人生活记录

可以作为轻展示或后续扩展，但不应喧宾夺主。

产品发展顺序应是：

1. 先把耕作记录系统做稳。
2. 持续完善高频实操指引；基础功能稳定后，推进地区经验地图与商社会员。
3. 后续再扩展相似气候、地区化指引、自家地块平面图、自动化灌溉、传感器、AI 辅助和机器人照料。
4. 其他生活内容只做轻量展示，不作为当前核心建设方向。

长期延伸方向是帮助家庭农场和小型生产者记录真实生产过程，并把这些过程转化为可追溯、可展示、可推广的商业信用。

规则：

* 当前首先服务个人长期耕作记录。
* 家庭农场方向是现有档案、记录和时间线的自然延伸。
* 不把产品改成普通广告平台或农业电商平台。
* 不直接进入大型农场重型 ERP。
* 商业信用来自长期、真实、连续、可验证的生产记录。
* 真实记录以后可以转化为生产履历、二维码页面、公开展示和集市推广内容。
* 面向小种植户、家庭农场、苗圃和小团体的商社会员属于下一阶段；重型生产管理仍是更远期方向，不能写成当前已具备的功能。

---

## 二、产品气质

产品应保持：

* 安静
* 长期
* 个人化
* 克制
* 不表演
* 不强社交
* 有自然感和生活感
* 适合全球用户理解和使用
* 经得起时间沉淀，越久越香醇

有时·耕作不是追求短期热闹、流量刺激或强社交互动的产品，而是一个长期积累型耕作记录系统。

产品设计应支持：

* 用户长期记录自己的耕作过程
* 档案随着时间变得越来越有价值
* 记录越久，越能看见土地、作物、气候、设施和人的变化
* 多年后的用户仍然能轻松回看、整理和导出自己的资料
* 每一条记录都能成为未来判断、回忆和经验积累的一部分

设计原则：

* 不追求短期热闹。
* 不用强打卡、强排名、强关注、强流量机制驱动用户。
* 不让用户为了发布而发布。
* 不让界面变成快速消费的信息流。
* 记录应安静、稳定、可回看、可整理。
* 数据结构应考虑长期维护和向后兼容。
* 用户数据应支持导出和迁移，避免被平台锁死。
* 多年积累后的档案、地图、作物记录、气候记录和照片，应比刚开始使用时更有价值。

理想状态：

用户使用一年后，能看到一个季节的变化。
使用三年后，能看到一块地的性格。
使用十年后，能看到自己与植物、土地和生活共同留下的轨迹。

---

## 三、核心文案

中文主文案：

你所照料的、陪伴着的生命，也滋养、成就着彼此。
有时，记录这些过程，让生命有迹可循。

留其间，守其度，顺其时，共养成。

整站介绍首页推荐展示（耕作子页可使用“有时·耕作”）：

LifeSpace·自然

你所照料的、陪伴着的生命，
也滋养、成就着彼此。

有时，记录这些过程，
让生命有迹可循。

留其间，守其度，
顺其时，共养成。

一个围绕耕作展开的生活空间。

英文参考文案：

The lives you care for and keep company with also nourish and shape you in return.
LifeSpace records these quiet processes, so life can leave its trace.

Stay with it.
Keep its rhythm.
Follow its season.
Grow together.

英文副标题参考：

LifeSpace for Cultivation

---

## 四、产品规则

* 不要把项目做成强社交平台。
* 不要做成泛生活社交平台。
* 不要把产品整体做成普通云盘或同步工具。
* 用户感知上应是“我的空间 / 我的档案 / 我的记录”。
* 在收费、开通、迁移和权限说明中，必须清楚说明：本地离线免费、Plus 云会员收费并包含云空间；公开内容可浏览，云端写入、发布及社区互动需有效云权益，集市咨询遵守注册用户规则。用户直接新建的云端项目和记录默认公开，创建界面必须清楚展示并允许修改。云端项目切换公开或私密时，现有记录及今后新增记录统一跟随；本地项目转到云端时由用户一次选择公开或仅自己可见，项目与全部记录统一采用该选择。
* Discover / 发现页只展示公开内容。
* 不要突出点赞、粉丝、排名、流量竞争。
* “歇歇 / 轻聊天”如果存在，应是轻入口，不是核心功能。
* 后续“作物库”可以作为辅助工具，但不要做成大而全的权威植物百科。
* 产品应优先服务用户自己的长期耕作实践，而不是做成普通农业资讯站。

---

## 五、注册规则

网页端可以支持注册和登录，不需要禁止或刻意弱化注册入口。

注册流程应简单、克制，服务于用户建立自己的有时·耕作空间，而不是做强营销转化。

注册是推荐路径，但不能成为本地离线记录的硬门槛。大陆无 VPN 用户可能无法稳定完成注册；如果注册失败或网络不可用，应允许用户先本地使用。此处的“本机离线缓存”指 App 私有本地存储中的项目、记录和图片缓存，不是自动写入手机系统相册。

用户身份字段统一叫“用户类型”：

| 身份 | 显示名称 | 说明 |
| --- | --- | --- |
| 未登录 | 游客 | 本机记录可直接使用，注册不是离线门槛 |
| 已注册、未获得有效云权益 | 注册用户 | 本地永久免费；正常账号不能显示“未设置” |
| 已主动领取且云体验有效 | 云体验 | 一次性体验，明确显示容量与有效期 |
| 已付费云会员 | Plus | 会员说明页解释为“LifeSpace Plus · 云会员”；到期状态单独显示，不暗示权益仍有效 |
| 下一阶段长期经营主体 | 商社会员 | 尚未开发开放，不在当前购买选项中出售 |

`admin` 是独立管理角色，不是付费用户类型。会员信息加载中、读取失败和未领取体验必须分别处理，不能把读取失败当成“注册用户”或“未设置”。

当前权限目标如下；联网浏览仍需网络，离线内容范围见本地离线规则：

| 权限 | 游客 | 注册用户 | 有效云体验 | 有效 Plus |
| --- | --- | --- | --- | --- |
| 浏览公开发现、公开记录和集市 | 是 | 是 | 是 | 是 |
| 本机离线项目、记录和图片 | 是 | 是 | 是 | 是 |
| 指引目录、名称和分类 | 是 | 是 | 是 | 是 |
| 指引基础概要及少量核心静态参数 | 否 | 是 | 是 | 是 |
| 已有云权益指引内容、参数及相关种植记录 | 否 | 否 | 是 | 是 |
| 浏览作者主动公开的经验卡 | 是 | 是 | 是 | 是 |
| 从本人云端项目创建和管理经验卡 | 否 | 否 | 是 | 是 |
| 评论、回复、点赞、有帮助反馈和关注 | 否 | 否 | 是 | 是 |
| 集市咨询和联系发布者 | 否 | 是 | 是 | 是 |
| 云端私密保存、同步和公开发布 | 否 | 否 | 是 | 是 |
| 集市“发布中”额度 | 0 | 0 | 3 | 30 |
| 云空间容量 | 0 | 0 | 30MB | 1GB |

指引内容深度以实际已提供内容为准，访问权限不等于已建成完整实操知识库。导出本人资料和本机记录始终属于用户的数据管理能力，不是 Plus 独占权益。

补充规则：

* 新正式账号注册成功后生成永久公开编号，规范格式为 `LSa + 四位注册年份 + 三位流水码`，例如 `LSa2026001`，不带横杠。
* 三位流水码先使用 `001` 至 `999`；超过后使用 `A01` 至 `A99`、`B01` 等字母段，跳过 `I`、`O`。编码不得截断、循环或重复；字母段容量用尽前另行确定兼容扩展方案。
* 旧格式 `LSa-2026-0001` 等编号必须兼容解析、查询、确认操作与导出。格式变更不重建账号、不改变 UUID、注册排名、数据所有权或本地关联，不给老用户重新发放账号身份。
* `a` 表示注册时的账户类别，不表示当前会员等级；以后升级 Plus 或商社会员，编号保持不变。
* 正式注册总序号跨年份、跨未来账户类别连续累加；账号删除后，编号和顺序不回收、不补位。
* 已明确标记的历史内部测试账号不获得正式公开编号，也不占正式注册总排名；它们保留原有容量、到期日和集市额度，不参与新的注册用户体验领取。不能把后来注册的账号都视为内部测试账号。
* 每个正式注册账号在邮箱确认后可主动领取一次30MB云端体验；体验从领取时开始计算，90天后到期，不能重复领取。
* 符合条件的新用户在“我的空间”或用户资料页应有容易找到的“开启云端体验”入口，会员详情页提供同一领取流程。身份和容量只展示，领取入口单列；未确认邮箱、已领取、容量预算不足或读取失败要给出对应状态，不能静默隐藏所有入口。
* 注册本身只创建账号和永久编号，不自动领取体验，也不自动上传或公开任何本地内容。
* 安全预算按平台实际Storage占用加上当前有效体验尚未使用的剩余额度计算；预计下一份30MB会越过700MB安全线时暂停新领取，但注册和本地免费使用始终开放。
* 体验到期或30MB容量用完后仍可继续本地记录；90天体验到期时停止新增云端内容、互动和发布，已有云端内容进入只读。
* 体验到期后另有90天处理期。用户可在此期间开通云会员、导出或把需要保留的数据转回本地；处理期结束后，系统通过安全删除队列清理从未转为有效付费会员的体验账号所拥有的云项目、记录、照片和集市内容。
* 自动清理不删除账号、个人资料、付款记录或本地数据。已转为有效付费会员的账号退出免费体验清理流程；开通云会员后容量升级为1GB。
* 面向用户的到期提示统一描述为“云端体验结束，转回本地使用”，不直接使用“删除云端数据”等措辞；同时必须说明只有已经保存在本机的内容会继续离线可用，尚未保存到本机的内容应在处理期内导出或转回本地，不能暗示系统已自动下载全部云端内容。
* 体验到期后的90天处理期以站内通知为主。待正式 SMTP 接入后，处理期内只在最终结束云端保留前7天发送一次关键邮件，其余到期、30天、开始和完成提醒只走站内通知；邮件发送失败不得阻断到期、只读或后续处理流程。
* 曾经至少有一笔确认成功且未被撤销的付费会员记录，账号即永久退出免费体验自动清理流程。会员以后不续费时，未由用户主动删除的云端内容长期保留并进入只读，可随时、多次保存到本机，也可查看、导出、删除和从公开改为私密；不能新增、编辑、上传、互动、重新发布或从私密改为公开，续费后恢复云端写入与同步。
* 本地能力始终可用，不增加“启用本地”的总开关。云端状态有效时，项目属性中的操作叫“转到本地”：先把项目、记录和图片下载到不可见的临时区，全部校验成功后再将云端项目移出可用云空间，本地成为唯一可编辑版本；任一步失败都必须保留原云端项目且不得破坏已有本机内容。
* 同一项目始终只允许一个可编辑主版本。正常状态下云端转本地成功后不再保留可编辑云端版本；本地重新转到云端成功后，本地版本从本地列表移除。两边都不得自动双向同步，也不得在两个可编辑版本之间反复覆盖。
* 付费会员到期未续费时，项目操作仍叫“保存到本机”，只生成或更新本机副本，承诺长期保留的云端原件继续只读。体验结束且从未付费的账号在处理期内将项目“转回本地”，成功后云端项目移出可用云空间；未处理内容到处理期结束按既定规则清理。任何状态都不增加“是否删除云端”的询问，删除只作为用户主动管理操作。
* 付费到期用户在同一设备再次保存同一云端项目时，必须识别来源并更新原本机副本，不得生成重复项目；更新前提示本机后续修改会被覆盖。不同设备可以各自保存一份只读云端内容的本机副本。
* 历史已明确标记的 4 个内部试用账号保留原到期日、原容量和原集市额度；到期后转为本地免费权限。该历史数量不代表当前全部用户数量。
* 现有内部试用账号不参加上述正式账号领取和自动清理规则；不得借新规则重写其当前容量、日期和集市额度。
* `admin` 是内部管理角色，不是对外会员种类。
* 旧的 `large` 和 `seller` 只保留数据库兼容结构，当前不展示或销售。
* 注册用户的少量核心静态参数只包括日照评分、是否需要搭架，以及盆栽、室内、阳台适配评分；必须经窄化接口返回，不能直接开放参数表整行读取。
* 集市咨询属于注册用户与发布者之间的交易联系，不属于社区互动；注册用户可咨询，社区评论、回复、点赞、有用反馈和关注仍只开放给有效云权益用户。

本地离线规则：

* 数据只保存在当前设备的 App 私有存储中。
* 用户可以在 App 内离线查看这些项目、记录和图片。
* 离线仍使用正常“我的空间”本地界面与导航；本地项目详情、属性和添加记录的结构应与云端对应页面一致，不能只提供一个独立简陋备用表单。
* 未登录、断网和云端暂不可达时，都能查看、新建、编辑和删除本地项目及记录，能拍照、从相册添加和查看已保存图片；关闭 App、重启设备后继续可用。
* APK 内置轻量指引目录、名称和分类，新建本地项目可选择关联指引，也可自定义名称；离线不依赖实时拉取目录。完整图文指引按实际缓存与联网能力提供，不为离线一次打包全部内容。
* 在线本地与 APK 离线本地必须识别同一批项目、记录和照片；语言切换、账号关联和恢复网络都不得清空或复制出另一套资料。
* 不应默认自动写入系统相册。
* 用户从相册选图时，App 可以保存一份压缩副本 / 缓存副本用于记录展示。
* 用户在 App 内拍照时，默认保存到 App 内部记录缓存。
* 此后新增的记录照片在设备端统一生成标准版：最长边不超过 1800px、质量 82%，小图不放大；本地与云端使用同一标准。
* 只处理此后新增的照片，不批量重压已有照片；从本地转到云空间时复用 App 内标准版，不再次有损压缩。
* 是否同时保存到系统相册，应作为可选设置，默认关闭。
* 网页 / PWA 不能可靠控制系统相册时，不得显示一个无法兑现的自动保存开关；“同时保存原图到系统相册”由具备原生相册能力的 App 实现。
* 只有用户主动选择“保存到相册”或“导出”时，才写入系统相册或文件目录。
* 删除 App 内记录时，应删除 App 内缓存；不应默认影响用户系统相册里的原图。
* 如果用户从相册选择的原图后来被删除，App 内记录仍应能显示自己的缓存副本。
* 云端不可达时，本轮离线新增内容保存在本机，不自动上传。云端提交失败应保留可恢复的本地内容并如实显示状态，不能把失败标记为已同步；恢复网络不自动覆盖本地或云端资料。
* 单条记录累计照片数量不设产品上限；每次选择、添加或上传最多 10 张，可以分多次添加。
* 照片跨多个日期时，用户可选择按照片日期拆分，同一天的照片生成同一条记录；也可选择不拆分，合并记录采用所选照片中的最新日期。
* 无论是否拆分，每张照片自身的原始拍摄时间都应保留，供未来生长线和短视频按真实时间排序。

注册后用户应进入：

* 我的空间
* 我的档案
* 我的记录

不要把注册后的第一体验设计成：

* 社交关注
* 推荐流
* 排名
* 粉丝引导
* 强发布引导

账号与 App 的关系：

* App 可以使用同一账号登录。
* App 可在登录后绑定本地空间。
* 不需要因为未来 App 绑定逻辑而禁止网页注册。
* 注册、登录、关联本机、领取体验和开通 Plus 都不自动上传本地数据；用户逐个项目主动转到云端，并明确选择该项目的公开方式。
* 注册不应强依赖 Google、GitHub、Gmail 或国外验证码服务。
* 当前继续使用邮箱账号，把确认、重发和找回密码做稳定；手机号、Google / Apple 等便捷登录作为后续独立方案，不另建一套割裂身份。
* App 内注册优先于跳转网页注册。
* 不要依赖 vercel.app 域名作为大陆用户唯一入口。

正式访问与邮件链路：

* 当前正式域名为 `https://life-space.uk`，主站按 Cloudflare 部署核对；旧 Vercel / workers.dev 地址不是正式用户入口。
* 注册确认、重发确认和重置密码使用统一正式回调配置，不能随临时访问域名或 APK 本地 origin 生成正式邮件回调。
* 同步核对 Supabase Site URL、允许的回调地址、邮件模板和验证码允许域名。只改最终跳转域名不足以证明首跳验证服务可达。
* 以真实无 VPN 网络验证“收邮件 → 验证 → 正式确认页 → 登录 / 返回 App”完整流程；确认页保持轻量，失败可重试，不能把失败当成成功。
* 访问偏慢先检查当前 Cloudflare 链路、首屏请求、图片和缓存；不能照搬已过时的 Vercel 区域调整，或承诺域名更换后所有网络都同样快。

APP 更新与本地资料保护：

* 官网下载和 App 内“检查更新”必须指向同一正式 APK。用户设置中显示已安装版本、检查结果及可升级入口；下载完成不能被当成安装完成。
* Android 正式应用保持 `com.youshi.cultivation`、既有签名身份与递增的 `versionCode`，通过系统安装流程覆盖升级；不得换包名生成第二个 App，也不得把网页快捷方式当成 APK 更新。
* 保留既有本地数据库名、数据归属和可访问的存储来源；如确需迁移，应先做增量兼容与失败回退，禁止删库重建、重置记录 ID 或静默覆盖照片。
* 升级验收必须覆盖已有项目、文字记录、照片、分期及账号关联；模拟中断或保存失败时，原资料仍能读取。构建成功不能替代真机覆盖安装验证。
* 用户不需要通过反复备份、卸载重装来完成正常升级；不在界面反复展示“不要卸载旧版”等说明。系统必要的安装确认和失败原因仍应正常显示。
* Android App 已安装用户的常用导航服务应用操作，不重复导向只为下载安装准备的介绍首页。官网保留正式 APK 下载；Google Play 与 iOS 原生分发分别规划，不因规划存在就宣称已上架。

---

## 六、公开与私密规则

本项目按数据所在位置、主动迁移选择与项目可见性管理公开范围。注册、领取体验和付费不触发公开或自动上传。

默认规则：

* 本地离线模式：本地离线免费，数据只保存在当前设备的新建档案、记录默认私密，不上传云端，不进入 Discover / 发现页，不支持多设备同步和云端备份。卸载应用、换设备、清理本地数据后可能丢失。
* 注册后本地离线模式：用户注册账号免费，但注册不等于上传云端；用户可以继续免费使用本地离线记录，数据仍只保存在当前设备，不占用云空间容量，不进入 Discover / 发现页，换设备仍然看不到，除非用户主动迁移或导入。
* 云端项目：直接新建云端项目默认公开，创建界面清楚显示可见性，并允许改成仅自己可见。项目的既定可见性决定其新记录默认状态；项目级公开 / 私密切换使现有及后续记录统一跟随，不保留相互矛盾的“云端一律默认私密”旧规则。
* 私密云端内容：属于有效云权益支持的保存能力，不进入 Discover / 发现页，不能被其他用户搜索、查看或评论。
* 公开云端内容：可以进入 Discover / 发现页、相关项目和求助流；访客能浏览，互动按用户权益校验。公开浏览与拥有公开发布资格是两个不同权限。
* 已有本地记录或已有私密云记录不得自动公开。
* 用户应能在创建或编辑时清楚看到当前可见性状态。
* 用户应能随时将档案或记录改为“仅自己可见”。
* 如果档案设为私密，其下属记录和媒体也不应被公开展示。

状态对应关系：

| 状态 | 数据位置 | 是否免费 | 是否公开 |
| --- | --- | --- | --- |
| 临时本地使用 | 当前设备 App 私有存储 | 免费 | 否 |
| 注册后本地离线 | 当前设备 App 私有存储 | 免费 | 否 |
| 云空间私密 | Supabase 云端 | 收费 | 否 |
| 公开发现 | Supabase 云端 | 收费 | 是 |

本地转云端的迁移规则：

* 只有用户主动逐个项目执行迁移，才上传该项目及其记录、图片。
* 用户在迁移时一次选择公开或仅自己可见，项目及其全部记录统一使用该选择；未迁移的其他项目保持原样。
* 注册、登录、关联本机、领取体验、付费和恢复网络都不能替用户作出迁移或公开选择。
* 迁移成功前保留本地原件；失败可以恢复，不能留下看似成功但缺照片或记录的结果。

公开内容限制：

* 不公开用户邮箱、手机号、支付信息、后台备注、系统 ID 等敏感信息。
* 不公开精确地址、家庭住址、门牌号、精确定位等敏感位置数据。
* 图片中的 EXIF GPS 不得随公开照片泄露。用于填写记录地点的坐标与公开图片分开处理；图片处理失败时，不能回退为直接公开带位置元数据的原图。
* Discover 只能读取被标记为公开的档案或记录。
* 私密内容不能通过 URL 猜测、搜索、接口或导出泄露。

产品原则：

* 上传云空间不等于公开。
* 公开发现必须由用户主动选择。
* 旧记录不得自动公开。
* 公开 / 私密切换应简单明确。
* 不要把公开机制设计成强社交、流量竞争或排名机制。

求助规则：

* 求助不是独立论坛，而是记录的一种状态。
* 用户从某一条记录发起求助。
* 求助记录需要公开给其他用户查看。
* 私密记录点击求助时，必须提示：求助会公开这一条记录和相关图片。
* 用户确认后，才可以将该条记录设为公开求助。
* 用户的其他私密记录不得因此公开。
* V1 可以采用“项目壳公开 + 单条记录公开”的方式实现。
* 后续可支持“项目私密，但单条记录公开求助”的更细权限。
* 求助状态包括：无、求助中、已解决。
* 已解决记录应允许用户补充解决说明，沉淀为真实案例。

---

## 七、国际化与双语规则

LifeSpace·自然面向全球用户，以耕作为主线，至少提供中文与英文界面。

规则：

* 不要只按中国大陆本地网站来设计。
* 产品至少需要支持中文 / 英文双语切换。
* 中文和英文都是当前需要验收的界面，不能把英文只视为未来预留。
* 不要把大量中文文案直接硬编码在组件里。
* 新增和修改文案使用现有统一语言配置，同时检查中文和英文。
* 未登录时保留清楚、紧凑的语言切换入口；登录后的常用页面不重复占一排语言栏，统一到设置中切换。离线使用也能找到设置与语言切换。
* 时间、日期、价格、单位、货币、地区表达要考虑国际化。
* 用户界面的普通日期统一显示为 `YYYY/MM/DD`；需要精确时间的详情、评论、通知和操作记录显示为 `YYYY/MM/DD HH:mm`，普通界面不显示秒。原生日期输入值、数据库值和机器读取字段仍按技术标准保存，不直接作为用户界面格式展示。
* 页面设计不要依赖中国大陆特有平台、手机号注册、微信生态或国内访问环境。
* 当前邮箱注册、邮件确认和找回密码应兼顾大陆及海外用户。
* 不要使用过度本土化、网络流行语、强地域化表达。
* 产品气质应保持安静、自然、长期记录感，适合全球用户理解。
* 后续新增页面或组件时，应尽量把用户可见文案集中管理，方便翻译和双语切换。

---

## 八、主要模块

当前目标模块包括：

* 首页 / Landing Page
* 注册 / 登录
* 我的空间 / Dashboard
* 档案列表 / Archives
* 档案详情 / Archive Detail
* 时间线记录 / Records
* 媒体图片 / Media
* Discover / 发现页
* 云空间 / Pricing
* 导出 / Export
* 设置 / Settings

核心耕作模块包括：

* 作物档案
* 作物库
* 地块 / 花盆 / 种植床 / 小院 / 阳台
* 耕作记录
* 地图 / 平面图
* 本地气候
* 农法
* 设施制作
* 技术技能
* 自动化灌溉
* 传感器
* AI 辅助照料

轻扩展模块包括：

* 插花
* 写字
* 萌宠
* 健身
* 旅行
* 其他个人生活记录

轻扩展模块不应优先于耕作主线。

---

## 九、耕作内容范围规则

本项目当前阶段以“耕作 / 种植 / 养护”为主轴，不只是记录植物状态，也应逐步支持与耕作相关的农法、设施制作、技术技能和实践经验。

### 一、作物与种植记录

* 作物档案
* 播种、育苗、移栽、定植、修剪、采收记录
* 浇水、施肥、覆土、除草、病虫害处理记录
* 作物生长照片和时间线
* 作物库与基础养护参数

### 二、地块与空间管理

* 小院
* 阳台
* 花盆
* 种植床
* 温室
* 地块
* 水池
* 堆肥区
* 工具区
* 耕作地图 / 平面图

### 三、农法与种植方法

后续可支持与农法相关的内容记录和资料整理，例如：

* 自然农法
* 有机种植
* 覆盖种植
* 轮作
* 间作 / 混种
* 堆肥
* 绿肥
* 水培
* 基质栽培
* 育苗方法
* 土壤改良
* 病虫害生态防治
* 节气种植经验
* 地区化种植经验

### 四、设施制作与工具记录

后续可支持耕作设施和工具制作记录，例如：

* 育苗架
* 遮阳棚
* 防虫网
* 防雨棚
* 堆肥箱
* 花架
* 种植床
* 自动浇水装置
* 雨水收集
* 简易温室
* 水培设施
* 沼泽过滤
* 小型生态系统设施

设施内容可以作为独立档案，也可以关联到具体地块、作物或记录。

### 五、技术技能与实践知识

后续可支持用户记录和整理实践技能，例如：

* 嫁接
* 扦插
* 压条
* 分株
* 育苗
* 留种
* 修剪
* 授粉
* 采收
* 储藏
* 发酵
* 土壤判断
* 肥料制作
* 病虫害识别
* 设施维护
* 工具使用

这些内容不应做成权威百科，而应优先服务于用户自己的长期实践记录和经验积累。

---

## 十、地区、记录地点与地图规则

### 一、本轮地区与地点目标

| 数据 | 用途与默认值 | 修改与公开范围 |
| --- | --- | --- |
| 账号默认地区 | 为项目、记录及集市提供填写默认值；不把“注册地址”当作所有项目真实地点 | 用户可修改，修改不追溯覆盖旧项目和旧记录 |
| 种植项目的种植地区 | 新建种植项目必填，默认带入账号地区，可按实际种植地点修改；其他类别可沿用账号默认地区 | 保存到项目；未来地图与地区经验读取这里，公开最多到城市 / 区县等地区层级 |
| 记录地点 | 新建记录默认带入用户已设置地址 / 地区，是该条记录自己的可选信息 | 新建和编辑时可修改、清空；详细地址或精确坐标仅本人可见，不作为公开地图点 |

* 种植地区支持离线手工填写，不以 GPS、账号注册或在线地址解析作为填写前提。
* 旧项目缺少新增地区字段时仍能打开并保留全部内容，编辑时引导补齐，不因字段升级删掉、隐藏或重建旧项目。
* 一个人的多个种植项目可以在不同地方，不能用账号默认地区覆盖项目实际地区。
* 选择照片时，如照片含有拍摄位置，可由用户主动采用为记录地点；无元数据、拒绝定位或离线时仍可正常保存记录。
* 读取拍摄坐标不等于识别出文字地址。需要地址解析时按实际可用服务处理，失败可手填；异步结果不能覆盖用户已经编辑的地点。
* 记录地点在本地 / 云端迁移和本人导出时保留；公开项目、公开记录或经验卡不能把私有地点及 EXIF GPS 一并公开。

### 二、下一阶段：地区经验地图

地图首先服务“不同地区如何耕作”的经验检索，属于上线稳定后的重点功能，不在本轮开发完整地图。

* 入口位于“发现”顶部，与搜索同属发现工具；不加入底部主导航或种植分类按钮行。
* 第一版以地区、植物联合筛选和地区聚合点为主，按实际公开项目与经验展示；公开只到地区层级，不在家庭地址打点。
* 地图取项目的实际种植地区，并且只聚合允许公开的项目 / 经验。自家详细地址、私密记录和原始 GPS 不进入公开聚合。
* 有效 Plus 可使用地图。有效云体验内提供每账号一次的独立 14 天地图体验，从用户首次明确开启时计时；地图是云体验的子权益，不改变云端记录的 30MB / 90 天规则，也不在云权益结束后继续放行。
* 地图不再按早期提议开放整个 90 天。体验结束后可保留入口与说明，不提供受限查询的实际结果；是否提供轻预览在专题计划中明确。
* 普通注册用户可看到入口和权益说明，但没有正式地图查询权限；服务端也要检查权益，不能只隐藏前端按钮。
* 有一批跨地区的真实公开内容、注册与离线基本流程稳定后再开发，不用硬性等待固定会员人数。
* 相似气候、月份规律、地区化实操建议、天气与气候数据接入分阶段推进，不一次建成全球农业模型。
* 地图供应商、国内可达性、地理编码、费用与用量在开发时重新核实；讨论过的报价不能当作永久成本承诺。

### 三、更后续：自家地块地图 / 平面图

自家小院、阳台、花盆、种植床、水池或温室的空间布局仍保留为后续方向。可考虑手工平面图、区域摆放、地块历史和轮作记录，但它与“发现”中的地区经验地图是两种功能，不能用平面图代替已确认的地区地图计划。

---

## 十一、本地气候规则

本项目未来耕作模块应把“本地气候”作为重要参考层。

耕作记录不应只记录作物本身，还应逐步支持用户结合所在地气候、季节、天气和小环境进行长期判断。

项目相关的气候与地区经验优先使用该项目的实际种植地区；账号地区只提供初始填写默认值。

未来可支持：

* 用户所在地城市 / 地区
* 气候区
* 四季变化
* 节气
* 月平均温度
* 最低温 / 最高温
* 霜冻期
* 高温期
* 梅雨期
* 台风 / 暴雨期
* 干旱期
* 降雨量
* 空气湿度
* 日照时长
* 风向 / 风力
* 小院、阳台、地块的小气候备注

本地气候应能与以下内容联动：

* 作物库
* 播种时间
* 移栽时间
* 采收时间
* 浇水建议
* 遮阳建议
* 防寒建议
* 防雨建议
* 病虫害风险
* 地块 / 花盆 / 种植床
* 耕作地图 / 平面图
* 自动灌溉计划
* AI 辅助照料建议

地区化种植建议：

* 宁波地区播种建议
* 江南地区梅雨期管理
* 夏季高温遮阳
* 冬季防寒
* 台风前设施加固
* 南方潮湿环境病害预防
* 北方霜冻地区播种调整
* 不同地区同一作物的种植时间差异

数据来源分阶段处理：

第一阶段：

* 用户手动填写城市 / 地区
* 用户手动记录天气和气候感受
* 系统提供简单地区备注

第二阶段：

* 接入天气 API
* 获取当地温度、湿度、降雨、风力、天气预报
* 根据天气生成提醒

第三阶段：

* 结合传感器数据
* 结合地块小气候
* 结合历史记录生成更个性化建议

隐私规则：

* 不默认公开用户精确位置。
* 不默认公开家庭住址、门牌号、经纬度。
* 公开内容可以显示模糊地区，例如城市、气候区、种植区。
* 公开内容限于地区级信息；详细地址、家庭坐标及记录私有地点不进入公开气候或地图页面。
* Discover / 发现页不应泄露用户精确位置。
* 地图、照片、天气记录都要避免意外暴露住址。

当前阶段规则：

* 本地气候属于重要长期方向，但当前阶段不需要马上接入复杂天气 API。
* 当前阶段可以先预留城市 / 地区字段、气候备注字段、地块小气候备注、作物库中的地区化备注、记录中的天气 / 温度 / 湿度字段。
* 不要为了本地气候功能大规模重构当前代码。

---

## 十二、自动化灌溉与 AI 照料规则

本项目未来可逐步支持自动化灌溉、传感器数据、AI 辅助判断和机器人照料植物。

这些能力属于长期方向，不是当前阶段必须立即实现的功能。

### 一、自动化灌溉方向

未来可支持：

* 自动浇水设备记录
* 灌溉计划
* 定时浇水
* 按土壤湿度触发浇水
* 按天气预报调整浇水建议
* 按作物类型生成浇水提醒
* 记录每次自动灌溉时间、水量、设备状态
* 灌溉设备异常提醒
* 地块 / 花盆 / 作物与灌溉设备绑定

自动灌溉数据应能关联到：

* 用户
* 档案
* 作物
* 花盆
* 种植床
* 地块
* 耕作地图 / 平面图
* 时间线记录

### 二、传感器方向

未来可支持接入：

* 土壤湿度
* 空气温度
* 空气湿度
* 光照强度
* 土壤温度
* 水位
* 水质
* pH
* EC
* 设备电量
* 设备在线状态

传感器数据应作为辅助记录，不应替代用户自己的观察和判断。

### 三、AI 辅助照料方向

未来可支持 AI 辅助：

* 判断植物状态
* 分析叶片异常
* 提醒浇水、施肥、修剪、采收
* 根据作物库和历史记录给出养护建议
* 根据天气和季节调整提醒
* 根据用户的地块和作物情况生成任务清单
* 总结某个作物或地块的生长过程
* 发现异常趋势，例如长期缺水、过湿、光照不足等

AI 建议必须是辅助建议，不能作为绝对判断。

涉及食用安全、药用价值、病虫害用药、儿童和宠物安全时，应使用谨慎表述，不得生成未经验证的高风险建议。

### 四、机器人照料方向

未来可探索 AI 机器人或自动化设备参与植物照料，例如：

* 自动巡视
* 自动拍照记录
* 自动识别植物状态
* 自动浇水
* 自动补光
* 自动通风
* 自动提醒人工处理
* 与耕作地图、作物档案、设施档案联动

机器人照料不应替代用户的核心主体性。

产品仍应强调：

* 用户自己的照料
* 用户自己的观察
* 用户自己的长期记录
* 自动化只是辅助工具

### 五、安全与权限规则

自动化设备、AI 和机器人相关功能必须注意安全：

* 设备密钥、API token、webhook secret 只能服务端保存。
* 不能把设备控制密钥暴露到前端。
* 外部设备写入记录必须校验用户权限。
* 设备只能访问自己绑定用户的数据。
* 自动浇水、补光、通风等控制动作应有安全限制。
* 高风险操作应需要用户确认或设置明确规则。
* 系统应记录自动化操作日志。
* 设备异常时应有提示，而不是静默失败。
* 不要让 AI 自动执行高风险操作，除非用户明确授权。

当前阶段规则：

* 当前阶段只需在产品结构和数据库设计上预留可能性。
* 不要现在为了自动化灌溉、AI 机器人或物联网设备大规模重构现有代码。
* 如果后续开发，应分阶段进行。

---

## 十三、可计算的植物生态模型

本项目未来不应只建立静态的“作物库”或“植物百科”。

长期目标是建立一个“可计算的植物生态模型”。

这个模型的目标是：

* 把作物、地块、气候、土壤、水分、光照、季节、农法、设施、传感器和用户记录连接起来。
* 让系统不只是展示资料，而是能辅助判断、计算、提醒和推荐。
* 为未来自动化灌溉、AI 辅助照料、机器人照料植物提供数据基础。

### 一、模型核心对象

未来模型可逐步包含：

* 作物 / 植物
* 品种
* 地块 / 花盆 / 种植床 / 阳台 / 小院
* 土壤 / 基质
* 光照
* 水分
* 温度
* 湿度
* 风
* 降雨
* 季节 / 节气
* 本地气候
* 病虫害
* 农法
* 设施
* 工具
* 自动化设备
* 传感器
* 用户时间线记录

### 二、作物参数不只是文字说明

作物库中的内容应尽量结构化、参数化，而不是只写成文章。

例如：

* 光照需求
* 水分需求
* 肥力需求
* 通风需求
* 耐寒性
* 耐热性
* 耐湿性
* 耐旱性
* 酸碱适应范围
* 生长周期
* 播种温度
* 发芽温度
* 适宜生长温度
* 播种月份
* 移栽月份
* 采收月份
* 病虫害风险
* 管理难度
* 适合盆栽程度
* 适合地栽程度
* 适合新手程度

这些参数应为未来计算和推荐服务。

### 三、生态关系

未来可逐步记录作物之间、作物与环境之间的关系，例如：

* 伴生 / 混种关系
* 不宜相邻种植
* 轮作关系
* 前茬 / 后茬
* 竞争关系
* 遮阴关系
* 引虫 / 驱虫关系
* 需肥差异
* 根系深浅
* 对土壤的影响
* 对湿度和通风的影响

这些关系应支持未来地块规划、混种建议和轮作提醒。

### 四、计算目标

未来系统可基于植物生态模型进行计算，例如：

* 某作物是否适合当前地区和季节
* 某作物是否适合当前地块
* 当前是否适合播种、移栽或采收
* 当前是否需要浇水、遮阳、防寒、防雨
* 当前病虫害风险是否升高
* 当前地块是否适合继续种同一类作物
* 某两个作物是否适合混种
* 某地块下一季适合种什么
* 自动灌溉计划是否需要根据天气调整
* AI 是否需要提醒用户观察异常

### 五、数据来源

模型数据可以分阶段建立：

第一阶段：

* 用户自己的记录
* 手动填写的作物参数
* 本地经验备注
* 简单作物库

第二阶段：

* 本地气候数据
* 天气 API
* 地块和小气候记录
* 作物生长历史

第三阶段：

* 传感器数据
* 自动灌溉数据
* AI 图像识别结果
* 用户长期反馈
* 区域化种植经验

设计原则：

* 不做静态百科堆砌。
* 不把 AI 生成内容直接当作权威知识。
* 优先结构化、参数化、可计算。
* 所有计算结果都应作为辅助建议，不应替代用户判断。
* 涉及食用安全、药用价值、病虫害用药、儿童和宠物安全时，必须谨慎。
* 模型应服务于用户自己的耕作实践，而不是做成普通农业资讯平台。

当前阶段规则：

* 可计算的植物生态模型属于长期方向。
* 当前阶段不要为了这个目标大规模重构现有代码。
* 当前阶段只需在作物库字段、地块关系、气候记录、设施记录、自动化接口等方面预留可能性。

---

## 十四、数据库与权限规则

数据库相关信息请优先参考 `docs/database.md`。

重要原则：

* 不要擅自修改数据库结构。
* 不要擅自创建、删除或重命名表。
* 如果确实需要数据库变更，必须先提出方案，再创建 migration 文件。
* 不要直接操作生产数据库。
* 不要提交真实用户数据。
* 不要提交真实邮箱、真实图片、真实付款数据。
* 不要提交 Supabase service role key。
* 不要提交 PayPal secret。
* 不要提交真实 `.env.local`、`.env.production` 或其他真实密钥文件。

权限底线：

* 用户只能读取、修改、删除自己有权限访问的数据。
* 用户只能导出自己的数据。
* Discover 只能读取公开内容。
* 管理员功能必须依赖可靠的数据库权限或 RPC，不得只靠前端判断。
* 云会员权限不能只靠前端判断。
* 导出功能不能只靠前端判断。
* Storage 图片和文件必须限制用户只能访问自己有权限访问的资源。
* 不要弱化 RLS policies。
* 不要绕过 RLS。
* 不要把 service role key 暴露到前端。

特别注意：

* `get_my_membership`
* `is_app_admin`
* 所有 `admin_*` RPC 函数
* `media` bucket
* `avatars` bucket

这些对象当前需要结合 Supabase 后台定义、RLS、RPC、Storage policy 继续核对。

---

## 十五、环境变量规则

* `.env.example` 可以提交。
* `.env.local`、`.env.production`、真实 `.env` 文件不能提交。
* `NEXT_PUBLIC_*` 变量可以用于前端，但仍不能写入真实敏感密钥。
* `SUPABASE_SERVICE_ROLE_KEY` 只能服务端使用，禁止进入前端代码。
* PayPal secret、支付宝密钥、数据库直连密码、服务端密钥只能服务端使用。

---

## 十六、UI 与样式规则

* 手机端和电脑端样式必须隔离。
* 不要为了修手机端而破坏电脑端。
* 不要为了修电脑端而破坏手机端。
* 移动端要紧凑、清楚、易操作。
* 不要使用过大的卡片、图片、字体和行距。
* 档案卡片、记录卡片应保持清晰、克制。
* 长标题、长记录可以省略显示，但不要破坏布局。
* 不要把界面做成电商、资讯流或强社交平台风格。
* 首页、记录页、档案页应保持安静、自然、长期记录感。
* 设计应支持多年内容沉淀后的回看、整理和阅读。
* UI 不应只服务即时发布，而应服务长期档案。

本轮统一的移动端操作规则：

* 发现、经验、指引与我的空间的分类按钮统一字体、字重、大小、选中态和间距；发现页文字必要时可换行，其他页面保持一致。APP 使用“农设／虫鱼”短标签，网页保留“农法设施／虫鱼生态”。
* 求助筛选文字统一为“仅求助”，不再用“只看求助”。
* 我的空间“全部／云端／本地／＋项目”一行排下，不横向滑动；新增项目按钮与前三项保持可辨识的额外间距。
* 添加记录页面按项目、照片、记录内容等清晰组织，标题和照片数量不争抢同一行宽度；本地和离线表单都显式提供“拍照”和“从相册添加”，支持继续追加照片。
* 项目属性中的关联指引名用普通文字展示，不作下划线跳转链接；旁边保留修改名称 / 关联的操作，不能因改名重新创建项目或破坏已有记录。
* 项目状态与可见范围使用带文字状态的左右分段滑动控件，支持点击和滑动，不只依赖颜色；原有权限和公开确认仍生效。
* 记录三点菜单中，原先只有单个动作的二级选项直接放到一级，例如“设为私密／设为公开”；求助状态也减少不必要层级，保留当前状态、失败反馈及必要的公开确认。
* 项目生命周期统一用“期”：分期设置、开启分期、第 1 期等。界面不再混用“茬／轮／界”；数据库原有周期 ID 与历史记录关联保持兼容。农业“轮作”和生产“采收批次”等专门术语保留原义，不全局替换。
* 时间保存为明确 UTC / 偏移量的时间点，界面与输入采用手机当前时区；导出网页采用发起导出设备的时区并注明，结构化数据保留原始时间。照片优先读取 EXIF 时区，缺失时按设备时区解释并允许用户修改。文字编辑不改动原时间点；跨夏令时的“第几天”按日历日期计算。旧数据不得直接批量加减 8 小时。
* 用户界面不出现“当前字段无法持久化、本轮暂不保存”等开发说明；不能实现的操作应收起或给出用户能理解的状态，不提供会静默丢弃输入的表单。

会员管理页面：

* 手机端按概览、访问统计、会员注册、付款等分区切换，不把全部模块堆在同一长页；当前分区和选中用户必须清晰。
* 会员列表分页、稳定排序，查询和切换不因旧请求返回而跳动；用户详情分组呈现，返回时保留列表筛选与位置。
* 邮箱缺失明确显示为资料状态，不直接判定为可疑用户，也不能靠不存在的邮箱定位删除对象。
* 删除失败要保留可读原因；服务端依据目标用户 ID、操作者身份和既有权限处理，不能只靠前端隐藏按钮。
* 破坏性操作放在独立区域，确认目标账号，防止列表跳动或点错。修复删除功能不等于授权实际删除某个账号。

---

## 十七、云会员、云空间与支付规则

当前阶段不做自动续费订阅。

云会员开通目标是：

用户一次性支付年费后，网站自动或半自动开通一年云会员，不需要管理员直接手动改数据库，或只在异常订单中少量人工处理。

首发价格策略：

* 本地离线：免费。
* 基础云空间容量：1GB。
* 中国大陆固定首发价：人民币64元/年。
* 海外固定首发价：8美元/年。
* 人民币和美元为分区固定价格，不按实时汇率换算。
* 当前不做自动续费。

云会员包含：

* 云端私密空间。
* 多设备同步。
* 云端备份。
* 公开发现。
* 发布求助。
* 评论、回复、点赞、有帮助反馈和关注。
* 已提供的植物参数、养护指引与相关种植记录；经验卡的创建、管理、检索及当前已实现的本机 MP4 能力。尚未实现的比较、生长线等单独标为后续规划。
* 同时最多 30 条“发布中”的集市内容。
* 付费会员期满后，未由本人主动删除的云端资料长期只读保留，可随时、多次保存到本机。

本人资料导出、保存到本机属于用户的数据管理权，不作为付费独占功能。体验到期处理期与付费会员到期长期保留严格区分，遵守第五节规则。

会员价值与指引成熟度：

* 当前年费说明优先表达长期项目记录、云端保存、照片与记录组织、公开展示、互动、经验积累及资料可带走；容量如实标明，但不把服务仅描述成“买 1GB”。
* 实操指引仍在完善，属于增值能力，不能宣传成已经完整成熟的收费核心；“已有内容可访问”与“内容已达到实操深度”分开描述。
* 指引按高频主题逐步补到可操作程度：时机与条件、材料或比例、操作步骤、观察检查点、进入下一步的判断、常见失败及处理方法。
* 优先完善播种、移栽、修剪、扦插、高压、嫁接、施肥、堆肥、土壤、水肥管理、越冬度夏及常见生态问题；不为本轮界面修正一次补满全部知识目录。
* 会员页面及双语文案核对实际能力，已经实现的功能移除“未来可用”旧说明，未来地图、商社和专业订阅不能列成当前已交付权益。

经验卡的传播规则：

* 经验卡第一版由有效云会员从同一个云端项目中选择至少3条原始记录生成，不设累计上限；只保存标题、最多500字的可选详情描述、封面选择和来源关系，不复制记录正文与照片。详情描述是卡片级补充说明，不替代来源记录。
* 所有来源记录按原始时间排序，第一条为起点、最后一条为结果，中间为过程；沿用项目 / 来源导航，不在时间线下重复放“查看原记录”跳转。
* 作者主动公开的经验卡可由游客通过直接链接完整查看，也进入独立的“经验”公开信息流和经验搜索；“动态”搜索只搜索项目与记录，不混入经验卡。
* 发布前必须明确提示项目基础外壳、所选记录和照片将公开；其他未选择记录保持原来的可见性。
* 项目或任一来源记录改为私密、进入回收站或被删除时，经验卡必须立即停止公开。
* 经验卡不重复计算来源照片容量；后续平台生成的预览图和MP4属于衍生文件，不计入用户可见容量。
* MP4第一版由经验卡作者在当前设备的浏览器中生成，不经过服务器渲染，不上传云端，也不计入用户可见容量。
* “经验”公开信息流采用浏览器根据经验卡记录、原图和播放顺序即时生成的竖屏播放效果，只保存少量播放配置和所选媒体ID，不生成或上传一份公共MP4，因此不重复占用图片或视频存储。
* 所有被选记录都必须进入视频；每条记录的图片由用户逐条选择，也可以选择不使用图片，没有选择图片时使用文字背景。原记录文字自动生成烧录字幕，长文字自动分段，时长随记录数量和文字长度延长。
* 经验卡详情中MP4生成与预览区域放在时间线之前；有图片的记录画面应优先展示图片，字幕只占画面底部较小区域。
* 输出为静音H.264竖屏MP4；生成完成后在页面中循环预览，可直接调用设备文件分享或保存后上传到外部视频平台。
* 手机端项目详情的详情、属性及经验卡入口保持清楚，本地和云端对应页面使用一致结构；未实现的生长线不作为本轮可用功能宣传。个人空间项目页提供“我的经验卡”入口。
* 当前不承诺无限服务器视频渲染；以后是否增加云端渲染、保存或合理用量限制，应根据真实使用和成本另行确定。

注意：

* 不要承诺永久低价。
* 不要承诺无限云空间。
* 不要承诺所有未来功能都包含在基础云空间内。
* 面向个人用户的付费身份显示“Plus”，会员详情解释为“LifeSpace Plus · 云会员”；“云空间”只表示存储、同步和容量，不代替身份。
* 对外用户类型按第五节使用“游客／注册用户／云体验／Plus”；“本地免费”说明使用能力与价格，不作为另一套账号身份。
* 下一阶段团队经营层统一称为“商社会员”，功能入口用“商家主页／商品管理／成员管理”等直观文字。
* 购买资讯的“订阅”属于后续独立权益，不能混入用户类型或误写成用户出售资讯，也不等于启用个人会员自动续费。
* 数据库表、RPC、内部历史字段中已有 membership 命名可以保留，但用户可见文案应逐步迁移。

### 一、海外支付规则

海外支付优先使用 PayPal 一次性支付，而不是 PayPal 自动订阅。

规则：

* 用户支付 US$8 / year。
* 支付成功后，通过服务端 webhook / API 验证支付结果。
* 验证通过后，自动为用户开通 1 年云会员。
* 不要只以前端支付成功页面作为云会员开通依据。
* PayPal secret 只能服务端使用，禁止暴露到前端。
* PayPal 支付功能必须作为独立任务开发。
* 上线前必须使用 sandbox / 测试环境验证完整流程。

### 二、国内支付规则

中国用户云会员首发价为 ¥64 / 年。

当前阶段不做自动续费。

国内支付优先只选择一种方式：支付宝。

#### 第一阶段：半自动开通

如果暂时没有支付宝官方支付接入条件，先做半自动开通：

* 网站生成云会员订单。
* 付款页在订单生成后直接显示已配置的支付宝经营 / 商家收款码和收款方名称。
* 用户按订单金额通过支付宝付款，不需要通过邮件获取收款方式。
* 用户上传支付宝付款截图。
* 后台提供“确认付款并开通云会员”按钮。
* 管理员只核对付款截图并点击确认，不直接手动修改数据库。
* 系统自动写入或更新 memberships / user_memberships 表，为用户开通 1 年云会员。
* 如果收款码或收款方尚未配置，支付宝入口必须明确显示暂未启用，不得退回“发邮件获取付款方式”的流程。

第一阶段目标是减少人工操作，而不是强行接入完整支付接口。

#### 第二阶段：支付宝一次性支付自动开通

后续如果具备支付宝商户 / 产品开通条件，再升级为支付宝一次性年费支付：

* 用户支付 ¥64 / 年。
* 支付宝服务端异步通知付款结果。
* 网站后端校验订单号、金额、币种、支付状态、支付宝交易号和回调签名。
* 验证通过后，系统自动开通 1 年云会员。
* 不做自动续费。

#### 暂不接入微信支付

当前阶段不同时接入微信支付，避免增加复杂度。

微信支付可作为后续备选方案，仅在支付宝方案不合适或用户需求明显时再评估。

### 三、支付安全规则

* 支付模块必须作为独立任务开发，不要混在普通页面修改里。
* 支付成功必须由服务端确认。
* 云空间状态必须以后端数据库记录为准。
* 不允许只靠前端按钮返回结果开通云会员。
* 必须校验订单号、用户 ID、金额、币种、支付状态、平台交易号、回调签名。
* 必须处理重复 webhook / 重复回调，避免重复开通或重复延长。
* 必须记录支付订单状态。
* 必须记录云会员开通来源和支付平台交易号。
* PayPal secret、支付宝密钥、微信支付密钥、Paddle 密钥等都只能服务端使用，禁止暴露到前端。

建议数据库中至少保留：

* orders：订单表
* payments：支付记录表
* memberships / user_memberships：云会员状态表

orders 至少记录：

* id
* user_id
* plan_id
* amount
* currency
* status
* payment_provider
* created_at
* paid_at
* expires_at

payments 至少记录：

* id
* order_id
* user_id
* provider
* provider_transaction_id
* amount
* currency
* status
* raw_event_id
* created_at

memberships 至少记录：

* user_id
* plan
* status
* starts_at
* expires_at
* source_order_id
* updated_at

### 四、大空间方案

* 大空间方案不公开展示。
* 需要更大空间的用户联系管理员。
* 管理员邮箱：[yoyomibaobao@gmail.com](mailto:yoyomibaobao@gmail.com)。

### 五、集市规则

#### 集市定位

集市只负责：

* 信息展示
* 推广
* 引流
* 联系意向方

实际交易在其他平台或线下完成。

LifeSpace 当前不提供：

* 站内订单
* 站内付款
* 发货
* 退款
* 交易完成确认
* 成交担保

#### 发布类型

集市内容包括：

* 出售
* 交换
* 转赠
* 求购

四种类型共用同一套发布名额。

#### 用户可见状态

用户侧主要状态只包括：

* 发布中
* 已结束

不要增加：

* 已成交
* 交易完成
* 待付款
* 已付款
* 已发货
* 已退款

管理员隐藏和违规下架属于后台管理状态，不属于交易状态。

#### 发布名额

有效云会员最多同时拥有30条“发布中”的集市内容。

计数规则：

* 出售、交换、转赠、求购统一计数。
* 只统计当前“发布中”的内容。
* 已结束不占发布名额。
* 已删除不占发布名额。
* 编辑现有发布不会重复占名额。
* 新发布和将已结束内容重新发布，都必须检查名额。
* 名额限制必须由服务端或数据库执行，不能只依赖前端按钮；云体验按 3 条、有效 Plus 按 30 条区分。

#### 发布时长与商社会员

* 普通个人供需信息发布 30 天后自动变成“已结束”，用户可提前结束或一键续期；续期从操作时重新计算 30 天，并重新校验有效云权益及发布额度。
* 商社会员已列入下一阶段，面向小种植户、家庭农场、苗圃、工作室和小团体；当前不销售、不因更新总纲就启用这套权益。
* 下一阶段核心是长期商家主页、稳定商品目录、商品主动上下架、比个人更多的商品和多人共同维护。商品不执行个人集市 30 天自动结束规则，也不能只实现为“无限期普通帖子”。
* 商社会员采用“团队空间＋成员账号”：每位成员用自己的个人账号登录，由主账号所有者邀请并授权，不允许多人共用同一登录账号和密码。
* 商家主页、产品目录、专业发布、招募、咨询和转入团队的项目归团队空间所有；成员退出后内容仍保留，重要操作记录实际操作人。
* 首版成员角色可先采用“所有者／管理员／成员”，并逐项控制商家资料、产品、招募、咨询、数据与成员管理权限。
* 受邀成员获得的是团队管理权限，不自动获得个人云会员权益；成员若要使用个人云空间、公开个人项目或进行个人集市发布，仍需单独开通云会员。
* 分类与批量管理、供应状态、试用／试种招募及团队项目协作分阶段验证，不把全部设想堆进第一版。商社会员的长期产品档案与招募是两种不同内容；若后续实施招募，须设置名额、报名截止日期、任务周期、地区和条件、记录要求、回报与结果提交方式。
* 后续招募只有报名时仍有效的云权益用户可以申请；注册用户可以咨询，但不能仅凭注册申请。
* 发布人可根据报名者主动公开、或在本次报名中明确授权展示的种植经验选定约定名额；不得读取或披露报名者的私密项目、私密记录、联系方式或未授权数据。

#### 本人发布中的咨询界面

* 打开“我的发布”时，不显示向自己发送新咨询的输入框与发送按钮。
* 发布者仍可查看别人发来的咨询，并在对应咨询下回复；与编辑、结束、删除等本人管理操作分区呈现。
* 浏览他人发布时，符合条件的注册用户可正常咨询，沿用现有权限校验。

#### 与云空间容量的区别

明确区分：

* 1GB限制云端数据和媒体占用。
* 30条限制同时处于“发布中”的集市内容。
* 已结束内容不占发布名额。
* 已结束内容只要数据和图片仍被保留，就继续占用云空间容量。

#### 扩容包

集市发布扩容包目前只属于未来设想：

* 暂不实施。
* 不属于当前会员权益。
* 不进入当前开发范围。
* 不写入当前价格体系。
* 待真实用户普遍达到基础30条上限后再评估。

---

## 十八、导出规则

导出功能应只导出当前用户自己的数据。

允许导出：

* 用户自己的档案
* 用户自己的记录
* 用户自己上传的图片 / 媒体
* 用户自己的基础资料
* 可阅读的 HTML 文件
* 可选 JSON 元数据

禁止导出：

* 其他用户数据
* 其他用户评论
* 其他用户图片
* 支付秘密信息
* 系统隐私数据
* 管理后台数据

导出原则：

* 用户数据应尽量可带走。
* 不要把用户锁死在平台里。
* HTML 应作为主要可读导出格式。
* JSON 可以作为备份元数据，但不要只导出机器可读文件。

---

## 十九、冷存储与长期保存规则

本项目支持长期资料积累；当前不启用冷存储，也不因会员到期把资料转入不可直接使用的存储层。

以下冷存储内容仅保留为未来技术备选，不纳入本轮或下一阶段功能承诺。付费到期长期只读保留与体验处理期继续按第五节执行，不能用冷存储条款另起一套到期规则。

### 一、冷存储目标

冷存储的目标是：

* 降低多年历史图片和大文件的长期存储成本。
* 保护用户多年积累的耕作记录、图片、地图、气候数据、设施记录和档案。
* 让低频访问的历史资料仍然可以被找回、导出和恢复。
* 支持“经得起时间沉淀，越久越香醇”的长期产品气质。

### 二、适合未来冷存储的内容

未来可考虑进入冷存储或归档层的内容包括：

* 多年前的原始大图
* 长期不访问的历史媒体
* 已完成项目的旧记录图片
* 用户主动归档的档案
* 导出包备份
* 历史版本数据
* 低频访问的旧地图、旧附件和旧媒体

### 三、不应轻易冷存储的内容

以下内容不应在未确认前进入冷存储：

* 用户最近活跃项目的图片
* 当前档案封面图
* Discover / 公开展示正在使用的图片
* 用户经常访问的档案和记录
* 系统需要即时读取的缩略图
* 云空间状态、支付记录、权限数据等核心业务数据

### 四、用户体验原则

冷存储不能让用户感觉自己的资料被平台“偷偷拿走”或“打不开”。

未来如果启用冷存储，应满足：

* 用户能知道哪些内容已归档。
* 用户可以申请恢复或重新激活。
* 用户自己的数据仍应支持导出。
* 冷存储内容不应被视为删除。
* 恢复可能需要等待，但必须有明确提示。
* 不应因为云空间过期就立即删除用户历史资料。
* 不应在用户不知情的情况下静默丢失图片、附件或历史记录。

### 五、技术原则

未来冷存储设计应遵守：

* 热数据和冷数据分层。
* 原图和缩略图可分层处理。
* 缩略图可保留在热存储，原图可进入冷存储。
* 冷存储对象仍需保留 owner、archive、record、visibility 等权限关联。
* 冷存储恢复后仍必须经过权限校验。
* 私密内容进入冷存储后仍然必须私密。
* 公开内容进入冷存储后也不能绕过权限规则。
* 不要只保存无法理解的对象路径，应保留可追溯元数据。
* 不要把 public URL 作为长期不可迁移依赖，应优先保存 storage_path / object key 等可迁移路径。
* 如果未来接入对象存储分层、归档层或外部冷存储服务，必须作为独立任务设计。

### 六、与导出功能的关系

导出功能应优先保证用户自己的数据可带走。

如果某些媒体已进入冷存储：

* 导出时应提示需要恢复后再导出。
* 或提供异步导出任务。
* 不应静默漏掉冷存储文件。
* HTML 导出包应尽量保留记录文字、时间线、图片引用和缺失提示。
* JSON 元数据可记录冷存储对象状态、恢复状态和原始路径。

### 七、与云空间和容量的关系

冷存储不应被设计成惩罚用户的机制。

未来可以考虑：

* 本地离线或基础云空间保留一定热存储空间。
* 长期低频访问内容进入归档层。
* 大空间用户可获得更多热存储或更快恢复能力。
* 云空间过期后不应立即删除历史数据。
* 删除、归档、恢复、导出必须有清楚规则。

### 八、当前阶段规则

当前阶段不开发复杂冷存储功能。

当前阶段只需注意：

* 数据结构不要封死未来归档能力。
* media / records / archives 未来可增加归档状态。
* 不要把图片 URL 设计成无法迁移的永久依赖。
* 应逐步使用 storage_path，而不是只依赖 public URL。
* 不要在未确认前自动删除用户历史数据。
* 冷存储相关功能以后必须作为独立任务设计，不要混在普通页面修改、Storage policy 修改或支付功能中。

---

## 二十、基础统计与隐私规则

本项目可以收集基础使用统计，用于判断 APK 下载、App 首次打开、注册转化、云空间开通转化和本地功能使用趋势，但不得侵犯用户隐私。

统计目标：

* 统计 APK 下载量。
* 统计 App 首次打开量。
* 统计注册转化。
* 统计云空间开通转化。
* 统计本地功能使用趋势。

规则：

* 下载统计可以记录 APK 下载事件。
* App 首次打开时，如有网络，可以记录 `app_first_open`。
* 完全离线用户从未联网时，无法被统计，这是可接受限制。
* 如果用户离线使用后首次联网，可以补报匿名使用事件。
* 统计不应上传用户的记录正文、图片内容、精确地址、邮箱、手机号等敏感信息。
* 匿名统计应使用 `anonymous_id`。
* 登录用户可以附带 `user_id`。
* 本地记录数量、项目数量可以作为匿名聚合指标补报，但不得上传具体内容。
* 需要在隐私说明中说明会收集基础使用统计。

建议事件类型：

* `apk_download`
* `app_first_open`
* `app_open`
* `register`
* `cloud_space_opened`
* `local_project_created`
* `local_record_created`
* `local_data_bound_to_account`
* `local_data_synced_to_cloud`

---

## 二十一、内容安全规则

作物库、物种库、农法、设施、技术技能、AI 建议等内容不能宣称绝对医学、药用或食用安全。

涉及以下内容时必须谨慎：

* 有毒植物
* 药用价值
* 食用安全
* 儿童安全
* 宠物安全
* 病虫害用药
* 农药 / 杀虫剂 / 药剂
* AI 图像识别结果
* 自动化设备操作

规则：

* 不要把 AI 生成内容当作权威百科发布。
* 系统资料应允许标记来源、版本和是否已人工校对。
* 高风险建议必须保守表达。
* AI 建议只能作为辅助判断，不应替代用户自己的观察和专业判断。

---

## 二十二、商社会员与小型生产者方向

核心定位：

帮助家庭农场和小型生产者记录真实生产过程，并把这些过程转化为可追溯、可展示、可推广的商业信用。

商社会员的主页、商品目录与多人维护属于已确认的下一阶段，核心范围见第十七节。以下生产履历、追溯、任务及推广效果是逐步验证的延伸能力，不要求首版一次完成；资讯订阅独立规划。

目标用户包括：

* 家庭农场
* 小型苗圃
* 果园和采摘园
* 花木、多肉和种苗经营者
* 社区农场
* 特色种植户
* 小型生态农场
* 农产品工作室

未来值得验证的能力：

### 一、生产者主页

* 农场或生产者简介
* 经营类别
* 公开项目
* 真实生产过程
* 联系入口
* 外部交易平台入口

### 二、批次与生产履历

* 品种
* 地块或区域
* 种植批次
* 生长记录
* 采收批次
* 出圃批次

### 三、二维码追溯与展示

* 生产者选择公开哪些记录
* 客户扫码查看真实生产时间线
* 不自动公开全部内部资料
* 不把普通展示页宣称为法定认证系统

### 四、记录转推广

* 从真实项目或批次选择记录和图片
* 生成集市推广内容
* 避免重复填写和重复上传
* 推广内容仍然指向真实生产档案

### 五、基础团队能力

未来可逐步支持：

* 所有者
* 管理人员
* 工作人员
* 查看者
* 保留创建、修改和完成记录的操作痕迹

### 六、简单生产协作

未来可逐步支持：

* 任务
* 负责人
* 日期
* 投入品记录
* 采收记录
* 基础报表

### 七、推广效果

可以统计：

* 页面访问
* 集市曝光
* 联系按钮点击
* 外部链接点击
* 二维码扫码次数

由于交易在站外完成，不统计或宣称真实成交量、成交率。

### 八、当前不优先进入的方向

当前不优先开发：

* 大型农场完整 ERP
* 多基地复杂管理
* 财务会计
* 工资和工时核算
* 完整采购与仓储
* 农机设备管理
* 站内供应链订单系统
* 复杂合规报表
* 重型物联网平台
* 大量企业定制接口

原因：

* 开发和维护成本高
* 客户定制差异大
* 实施与售后要求重
* 容易偏离现有产品优势
* 当前优势是连接真实记录、长期时间线、对外展示和推广引流

家庭农场方向只有在个人版稳定并出现真实持续使用者后，才进入正式开发。

---

## 二十三、开发规则

每次修改应遵守：

* 一次只做一个小任务。
* 不要重构无关文件。
* 不要改无关页面。
* 不要在未确认前大规模重命名。
* 不要在未确认前改变产品方向。
* 不要在未确认前改变数据库结构。
* 修改前先说明计划。
* 修改后列出改动文件。
* 修改后说明如何测试。
* 如果 build / lint 失败，要说明原因。
* 如果需要我提供 Supabase schema、RLS、RPC 或 Storage policy，要明确列出缺失项。
* 如果存在安全风险，要明确指出。
* 涉及支付、权限、导出、Storage、管理员功能时，必须特别谨慎。
* 涉及未来功能时，先写方案，不要直接大规模开发。

---

## 二十四、当前阶段优先级

### 一、本轮修改与验收

1. 完成本地 / 离线查看、项目与记录维护、照片拍摄及保存、轻量指引名称选择；本地和云端对应页面结构一致。
2. 验证 APK 覆盖升级、断网重开、账号关联及恢复网络后本地资料完整，数据迁移失败能恢复，不能用“构建通过”代替数据验证。
3. 统一分类短名、字体和间距，修正“仅求助”、我的空间单行筛选和添加记录布局；未登录可切换语言，登录后在设置切换。
4. 项目属性的关联指引采用普通文字与修改入口，状态 / 可见性采用分段控件；记录菜单减少单动作二级层级，周期文案统一为“期”。
5. 启用可修改的记录地点和可主动采用的照片位置；区分账号默认地区、种植项目必填地区与记录私有地点，保留迁移和导出中的本人地点资料。
6. 本人集市发布去除向自己发新咨询的表单，保留查看与回复其他用户咨询。
7. 整理手机会员管理分区、稳定分页及用户详情，排查缺失邮箱和删除失败，避免选择跳动与误操作；不因排查就删除实际账号。
8. 用户类型显示为游客、注册用户、云体验、Plus；补齐容易找到的主动领取体验入口，保留 30MB / 90 天与一次性领取规则。
9. 账号编号改为无横杠三位流水码及后续字母段，兼容旧编号、全站注册序号和原有数据归属。
10. 注册确认、重发及找回密码使用正式域名，验证无 VPN 完整链路；按当前 Cloudflare 部署核对首屏、图片和缓存的速度问题。
11. 更新会员权益说明与双语文案，按实际成熟度描述指引；同步总纲中的规则，避免用未来能力证明当前已交付。
12. 完成权限、照片隐私、上传失败 / 容量、支付权益、体验到期、付费到期只读、导出及管理员操作的针对性回归；正式 SMTP、验证码和真实邮箱流程以实际配置和测试为准。

上述是本轮目标清单，不是完成声明。部署、数据库迁移与正式 APK 发布结果分别记录；不能因为用户看到了正确版本号或完成一次安装，就宣布所有功能和升级数据验收通过。

### 二、下一阶段与更后续

* 当前核心功能和上线流程稳定后，推进“发现 → 地区地图”及商社会员；专题细节分别维护在地图与商社会员计划中。
* 高频实操指引持续补充，不要求一次补齐全部植物、农法设施和虫鱼生态内容。
* Google Play 可以另行准备测试和上架材料；正式发布需先验证核心离线能力，并按届时要求核对分发、支付、隐私和账号注销。官网 APK 仍保留，iOS 原生分发单独规划。
* 资讯订阅、相似气候、地区化指引、便捷登录、自家地块平面图，以及后续自动化、AI 和植物生态模型，按需求与成本逐项确认。

暂时不要优先做：

* 大规模重构
* 强社交功能
* 复杂聊天区
* 大而全植物百科
* 自动续费订阅
* 生产数据库直接变更
* 复杂后台统计
* 复杂 IoT 平台
* AI 自动控制设备
* 机器人照料功能
* 大型农场完整 ERP
* 复杂家庭农场团队系统
* 站内交易和订单
* 集市扩容包
* 未经真实用户验证的家庭农场重型功能

## 2026-08-01 体验与导航补充

* 经验卡标题下的“项目名 · 系统名”分别承担导航：项目名打开来源项目记录详情；有系统指引时，系统名打开对应指引。记录时间线下方不再重复显示“查看原记录”。
* 手机个人空间的首个“项目档案”摘要行应整体可点击，进入我的项目页。
* 网页未登录时，集市页不重复显示全局导航中已有的登录和注册按钮；未登录首页优先在常见桌面视口内完整展示核心介绍和入口。
* 个人空间项目页的“我的经验卡”使用紧凑单行入口，不使用大面积说明卡；经验卡页面中的本地MP4预览需兼容原生 `loop` 不可靠的浏览器，在播放结束时主动回到开头继续播放。保存后的MP4文件是否循环由相册或外部平台决定。
* 经验卡详情页保持紧凑：视频生成区在最前，片头显示发布用户名并适当延长；记录日期与记录文字放在同一字幕区域，不单独悬浮在画面右上角。指引详情使用“概要与种植办法／经验卡／种植记录”三个同级页面。
* 项目详情展开档案时由“项目档案”文字承担打开入口，项目名只展示项目身份。关注列表中的项目名与系统名同一行，图文区域高度对齐。
* 发现页不得通过删减或强行压缩文字区来缩短卡片；应去除等高拉伸和固定空白，把更新时间、记录数、持续天数、评论数等轻量信息移入图片区域，正文信息保持完整并按内容自然高度排列。
* 未登录首页不使用大面积白色英雄卡框，主标题与口号字号保持克制；注册保留在首页主体操作区，登录保留在全局导航，顶部导航不重复显示注册。

## 2026-08-08 账号页与经验卡补充

* “我的经验卡”详情页负责查看成品和轻量管理：首行左侧为本机MP4，右侧为可原位修改的经验卡名称与概况；概况显示可点击的项目名和系统名、记录、创建、可直接切换的私密／公开状态，并提供占整行、可原位编辑的详情描述。生成竖屏MP4和分享MP4并入名称信息块；“删除经验卡”放在详情页最末尾单独一行。作者的经验卡网页分享与复制链接入口放在“我的经验卡”列表。
* 经验卡的云端封面、本机MP4选图和公开即时播放选图必须明确区分。本机MP4文件和本机片头不上传；公开即时播放只在云端保存媒体ID清单，不复制媒体二进制。经验卡封面通过经验卡编辑保存到云端，不得用“经验卡封面”文案误导用户。
* 经验卡创建与修改共用同一套“全部记录＋缩略图”勾选界面：项目全部已有记录按原始时间完整列出，每条记录下直接显示所属图片；创建时默认选择全部记录和图片并自动确定封面，修改时保留原选择，所有未选记录都可直接加入，包括较早记录、后来新增的记录和曾移出的记录。选择记录时默认全选其图片，取消记录时同步取消其视频图片。
* 经验卡编辑器不得在内部新建项目记录。作者详情页底部展开内容编辑，不另开独立编辑页；点击经验卡名称只修改名称。底部编辑区只负责记录、图片和封面选择及保存，不重复显示来源项目、成品预览或MP4区。
* 修改经验卡内容时必须在同一个数据库事务内保留原公开方式；已公开卡新增的来源记录需同步公开，避免公开访问中断。保存与公开切换互相独立。详情页“私密／公开”可以反复切换，公开仍需明确确认。
* 公开经验卡详情应持续提供收藏、“有帮助”、评论和“写评论”入口；本人不能给自己的经验卡收藏或标记“有帮助”，但可以查看统计、管理评论和参与回复。私密草稿不显示不可用的公开互动输入框。
* 桌面全局导航不单独放置突兀的“云会员／云空间”购买入口；会员入口归入用户资料页的“会员与容量”模块，具体受限页面可使用“了解云会员”作为解释入口。
* “云空间”只在说明真实存储位置、容量或本地转云端时使用；导航、账号身份和泛权限提示不要反复使用“云空间”。
* 用户资料页在桌面端与手机端统一使用并列模块入口：用户信息／会员与容量／个人空间／回收站／数据与安全；管理员另有会员管理入口。
* “导出我的记录”属于用户的数据管理能力，放在“数据与安全”，不得放进会员权益或会员容量区块，也不得让用户误以为只有付费会员才能导出。
* 未登录首页的会员入口命名为“会员类别与权限”，并用简短说明直接区分免费使用、云端保存与公开互动；注册保留在首页主体操作区，手机首页顶部与桌面导航不重复显示，登录入口保持可见。

## 2026-08-09 界面精简补充

* “我的经验卡”列表页不重复显示用途和新建规则说明，只保留页面标题、目录与必要空状态。
* 植物指引的“经验卡／种植记录”入口在有内容时显示实际数量，没有内容时不显示“0”。
* 浏览其他用户的项目时，进入该用户空间的入口放在项目标题右侧的用户名，不在项目档案卡内重复。
* 发现页搜索只是进入项目／记录／经验卡搜索的入口，使用标题旁的紧凑按钮，不占据完整一行。
* 经验卡详情不重复显示“经验卡／经验卡概况／竖屏MP4／经验卡管理”等分组标题。项目名和系统名保持可点击，但不显示末尾小箭头；概况统一为“项目／系统名／记录／创建／状态”五项，并在下方增加占整行的“详情描述”，作者在“状态”项直接切换私密／公开，访客只读已填写内容。记录只显示持续天数与记录条数，不再显示完整起止日期或单列图片数。作者操作区只保留一个“分享MP4”入口；作者的经验卡网页“分享／复制链接”入口放在“我的经验卡”列表，并代替列表原有的“取消公开”。已有本机MP4时生成按钮显示“重新生成MP4”，并显示该本机文件的实际大小。视频分享、保存和生成进度放在右侧信息区，不堆在视频预览下方。
* 作者详情页底部以“编辑经验卡”文字本身展开内容编辑，展开后原位显示“收起编辑”，不另设右侧按钮或空白分隔行。
* 经验卡状态统一显示“私密／公开”。公开提示只说明经验卡和所选记录会被他人看到、其他记录不变；设为私密只停止经验卡公开访问，来源项目和记录保持现状。
* “我的经验卡”列表与搜索结果的横向图文卡统一使用卡内留白的圆角方形缩略图，图片不贴住外框，也不随文字区高度拉伸；项目、记录和经验卡保持一致。
* 项目／记录／经验卡搜索结果继续使用同一套横向图文卡；桌面端两列，手机端一列。卡片保持图片、分类、标题、所属信息、摘要、日期、作者、地区及适用的轻量统计，但摘要最多显示两行，缩略图片和卡内间距保持紧凑；不恢复密集图标式旧列表。
* 搜索卡只显示用户真实填写的项目摘要、经验卡描述或记录文字；没有摘要时直接省略该行，不使用“项目刚刚开始”“以照片为主”“查看按原始时间排列”等占位说明。
* 用户空间中的“用户资料”直接打开资料页，不再使用“名片”名称或弹出小窗口；公开用户资料页保持紧凑，头像、身份、统计和操作集中在首屏。
* 浏览其他用户的项目时，项目标题右侧使用清晰的“进入用户名的空间”按钮式入口，不只显示容易被忽略的用户名。
* 页面标题下不重复展示只是在复述标题或控件用途的副标题；权限、隐私、离线存储、数据风险、失败原因和必要操作指引仍须保留。
