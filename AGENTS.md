# AGENTS.md

## 一、项目核心定位

本项目的主品牌是：有时。
当前核心产品名称是：有时·耕作。
英文参考名称是：LifeSpace for Cultivation。
产品副标题是：一个围绕耕作展开的生活空间。

当前阶段，整个 App 应围绕“耕作 / 种植 / 养护”展开。

它不是泛生活记录平台，也不是强社交社区。
其他个人生活内容可以存在，但只作为轻展示、轻扩展，不应与耕作主线同等重要。

品牌层级为：

* 主品牌：有时
* 当前产品：有时·耕作
* 产品解释：一个围绕耕作展开的生活空间
* 英文参考：LifeSpace for Cultivation

以后不要把主标题写成“有时。耕作 生活空间”。
正式中文名称优先使用“有时·耕作”。
“生活空间”作为产品解释和副标题使用，不强行挤进主标题。

核心主轴包括：

* 作物档案
* 地块 / 花盆 / 种植床 / 小院 / 阳台记录
* 时间线耕作记录
* 图片与媒体记录
* 播种、育苗、移栽、修剪、施肥、浇水、采收记录
* 病虫害、天气、土壤、光照、水分等观察
* 作物库
* 农法与种植方法
* 设施制作与工具记录
* 技术技能记录
* 耕作地图 / 地块平面图
* 本地气候
* 自动化灌溉
* 传感器数据
* AI 辅助照料
* 未来机器人照料植物
* 可计算的植物生态模型

其他内容，例如：

* 插花
* 写字
* 萌宠
* 健身
* 旅行
* 其他个人生活记录

可以作为轻展示或后续扩展，但不应喧宾夺主。

产品发展顺序应是：

1. 先把耕作记录系统做稳。
2. 再完善作物库、地块地图、本地气候、农法设施、技术技能。
3. 后续再扩展自动化灌溉、传感器、AI 辅助和机器人照料。
4. 其他生活内容只做轻量展示，不作为当前核心建设方向。

长期延伸方向是帮助家庭农场和小型生产者记录真实生产过程，并把这些过程转化为可追溯、可展示、可推广的商业信用。

规则：

* 当前首先服务个人长期耕作记录。
* 家庭农场方向是现有档案、记录和时间线的自然延伸。
* 不把产品改成普通广告平台或农业电商平台。
* 不直接进入大型农场重型 ERP。
* 商业信用来自长期、真实、连续、可验证的生产记录。
* 真实记录以后可以转化为生产履历、二维码页面、公开展示和集市推广内容。
* 家庭农场方向属于长期延伸，不是当前已经具备的功能。

---

## 二、产品气质

产品应保持：

* 安静
* 长期
* 个人化
* 克制
* 不表演
* 不强社交
* 有自然感和生活感
* 适合全球用户理解和使用
* 经得起时间沉淀，越久越香醇

有时·耕作不是追求短期热闹、流量刺激或强社交互动的产品，而是一个长期积累型耕作记录系统。

产品设计应支持：

* 用户长期记录自己的耕作过程
* 档案随着时间变得越来越有价值
* 记录越久，越能看见土地、作物、气候、设施和人的变化
* 多年后的用户仍然能轻松回看、整理和导出自己的资料
* 每一条记录都能成为未来判断、回忆和经验积累的一部分

设计原则：

* 不追求短期热闹。
* 不用强打卡、强排名、强关注、强流量机制驱动用户。
* 不让用户为了发布而发布。
* 不让界面变成快速消费的信息流。
* 记录应安静、稳定、可回看、可整理。
* 数据结构应考虑长期维护和向后兼容。
* 用户数据应支持导出和迁移，避免被平台锁死。
* 多年积累后的档案、地图、作物记录、气候记录和照片，应比刚开始使用时更有价值。

理想状态：

用户使用一年后，能看到一个季节的变化。
使用三年后，能看到一块地的性格。
使用十年后，能看到自己与植物、土地和生活共同留下的轨迹。

---

## 三、核心文案

中文主文案：

你所照料的、陪伴着的生命，也滋养、成就着彼此。
有时，记录这些过程，让生命有迹可循。

留其间，守其度，顺其时，共养成。

首页推荐展示：

有时·耕作

你所照料的、陪伴着的生命，
也滋养、成就着彼此。

有时，记录这些过程，
让生命有迹可循。

留其间，守其度，
顺其时，共养成。

一个围绕耕作展开的生活空间。

英文参考文案：

The lives you care for and keep company with also nourish and shape you in return.
LifeSpace records these quiet processes, so life can leave its trace.

Stay with it.
Keep its rhythm.
Follow its season.
Grow together.

英文副标题参考：

LifeSpace for Cultivation

---

## 四、产品规则

* 不要把项目做成强社交平台。
* 不要做成泛生活社交平台。
* 不要把产品整体做成普通云盘或同步工具。
* 用户感知上应是“我的空间 / 我的档案 / 我的记录”。
* 在收费、开通、迁移和权限说明中，必须清楚说明：本地离线免费、云会员收费并包含云空间、云空间可以私密、公开发现需要云会员、上传云空间不等于公开、旧记录不会自动公开。
* Discover / 发现页只展示公开内容。
* 不要突出点赞、粉丝、排名、流量竞争。
* “歇歇 / 轻聊天”如果存在，应是轻入口，不是核心功能。
* 后续“作物库”可以作为辅助工具，但不要做成大而全的权威植物百科。
* 产品应优先服务用户自己的长期耕作实践，而不是做成普通农业资讯站。

---

## 五、注册规则

网页端可以支持注册和登录，不需要禁止或刻意弱化注册入口。

注册流程应简单、克制，服务于用户建立自己的有时·耕作空间，而不是做强营销转化。

注册是推荐路径，但不能成为本地离线记录的硬门槛。大陆无 VPN 用户可能无法稳定完成注册；如果注册失败或网络不可用，应允许用户先本地使用。此处的“本机离线缓存”指 App 私有本地存储中的项目、记录和图片缓存，不是自动写入手机系统相册。

首发权限只对外展示三种用户状态：

| 权限 | 游客（未注册） | 本地用户（免费） | 云会员 |
| --- | --- | --- | --- |
| 浏览公开发现、公开记录和集市 | 是 | 是 | 是 |
| 本机离线项目、记录和图片 | 是 | 是 | 是 |
| 植物指引目录、名称和分类 | 是 | 是 | 是 |
| 植物指引基础概要 | 否 | 是 | 是 |
| 少量核心静态参数 | 否 | 是 | 是 |
| 完整参数、生长周期和完整养护指引 | 否 | 否 | 是 |
| 指引中的相关种植记录 | 否 | 否 | 是 |
| 未来：作者主动公开的单张经验卡（直接链接或二维码） | 是 | 是 | 是 |
| 未来：创建经验卡、经验卡库、搜索、筛选和比较 | 否 | 否 | 是 |
| 评论、回复、点赞、有帮助反馈和关注 | 否 | 否 | 是 |
| 集市咨询和联系发布者 | 否 | 是 | 是 |
| 云端私密保存、同步、备份和公开发布 | 否 | 否 | 是 |
| 集市“发布中”额度 | 0 | 0 | 30 |
| 云空间容量 | 0 | 0 | 1GB |

补充规则：

* 新正式账号注册成功后生成永久公开编号，格式为 `LSa-注册年份-全站正式注册总序号`，例如 `LSa-2026-0001`。
* `a` 表示注册时的账户类别，不表示当前会员等级；以后升级云会员或商业会员，编号保持不变。
* 正式注册总序号跨年份、跨未来账户类别连续累加；账号删除后，编号和顺序不回收、不补位。
* 现有账号属于内部测试账号，不获得正式公开编号，不占正式注册总排名，也不占首批20个体验名额。
* 首批20名正式注册用户，在平台存储安全线允许时自动获得30MB云空间体验额度；体验额度不设6个月期限。
* 安全预算按平台实际Storage占用加上体验用户尚未使用的剩余额度计算；预计下一份30MB会越过700MB安全线时提前暂停赠送。正式总排名仅前20位有资格，注册本身始终开放。
* 正式总排名第21位起不再获得首批体验额度，但仍是可正常使用的本地用户；30MB用完后也仍可继续本地记录，开通云会员后容量升级为1GB。
* 现有 4 个内部试用账号保留原到期日、原容量和原集市额度；到期后转为本地免费权限。
* 过期账号的已有云端内容不删除，仍可查看、导出和删除，但不能新增云端内容、互动或重新发布集市。
* `admin` 是内部管理角色，不是对外会员种类。
* 旧的 `large` 和 `seller` 只保留数据库兼容结构，当前不展示或销售。
* 本地用户的少量核心静态参数只包括日照评分、是否需要搭架，以及盆栽、室内、阳台适配评分；必须经窄化接口返回，不能直接开放参数表整行读取。
* 集市咨询属于注册用户与发布者之间的交易联系，不属于社区互动；本地用户可咨询，社区评论、回复、点赞、有用反馈和关注仍只开放给有效云会员。

本地离线规则：

* 数据只保存在当前设备的 App 私有存储中。
* 用户可以在 App 内离线查看这些项目、记录和图片。
* 不应默认自动写入系统相册。
* 用户从相册选图时，App 可以保存一份压缩副本 / 缓存副本用于记录展示。
* 用户在 App 内拍照时，默认保存到 App 内部记录缓存。
* 此后新增的记录照片在设备端统一生成标准版：最长边不超过 1800px、质量 82%，小图不放大；本地与云端使用同一标准。
* 只处理此后新增的照片，不批量重压已有照片；从本地转到云空间时复用 App 内标准版，不再次有损压缩。
* 是否同时保存到系统相册，应作为可选设置，默认关闭。
* 网页 / PWA 不能可靠控制系统相册时，不得显示一个无法兑现的自动保存开关；“同时保存原图到系统相册”由具备原生相册能力的 App 实现。
* 只有用户主动选择“保存到相册”或“导出”时，才写入系统相册或文件目录。
* 删除 App 内记录时，应删除 App 内缓存；不应默认影响用户系统相册里的原图。
* 如果用户从相册选择的原图后来被删除，App 内记录仍应能显示自己的缓存副本。
* 云空间模式下，新记录属于云空间，但本机仍保留 App 内离线缓存；断网时先保存在 App 本地，联网后再同步到云端。
* 单条记录累计照片数量不设产品上限；每次选择、添加或上传最多 10 张，可以分多次添加。
* 照片跨多个日期时，用户可选择按照片日期拆分，同一天的照片生成同一条记录；也可选择不拆分，合并记录采用所选照片中的最新日期。
* 无论是否拆分，每张照片自身的原始拍摄时间都应保留，供未来生长线和短视频按真实时间排序。

注册后用户应进入：

* 我的空间
* 我的档案
* 我的记录

不要把注册后的第一体验设计成：

* 社交关注
* 推荐流
* 排名
* 粉丝引导
* 强发布引导

未来 App 上线后：

* App 可以使用同一账号登录。
* App 可在登录后绑定本地空间。
* 不需要因为未来 App 绑定逻辑而禁止网页注册。
* 注册不等于上传云端，开通云会员后才同步本地数据。
* 开通云会员后，本地数据同步为云空间私密，不得自动公开。
* 注册不应强依赖 Google、GitHub、Gmail 或国外验证码服务。
* 后续应预留手机号注册 / 国内邮箱验证码注册。
* App 内注册优先于跳转网页注册。
* 不要依赖 vercel.app 域名作为大陆用户唯一入口。

---

## 六、公开与私密规则

本项目采用“按使用模式决定默认可见性”的策略。

默认规则：

* 本地离线模式：本地离线免费，数据只保存在当前设备的新建档案、记录默认私密，不上传云端，不进入 Discover / 发现页，不支持多设备同步和云端备份。卸载应用、换设备、清理本地数据后可能丢失。
* 注册后本地离线模式：用户注册账号免费，但注册不等于上传云端；用户可以继续免费使用本地离线记录，数据仍只保存在当前设备，不占用云空间容量，不进入 Discover / 发现页，换设备仍然看不到，除非用户开通云会员并同步。
* 云空间私密模式：云空间私密属于云会员权益，数据上传到用户自己的云空间，支持多设备同步和云端备份，新建云端档案、记录默认私密。上传云空间不等于公开，云空间私密内容不进入 Discover / 发现页，不能被其他用户搜索、查看或评论。
* 公开发现模式：公开发现属于云会员权益。用户明确选择公开发现模式后，新建云端档案、记录可以默认公开。公开内容可以进入 Discover / 发现页、百科相关项目、求助流，并可被其他用户查看、评论。用户应能在新建或编辑时改为私密。
* 已有本地记录或已有私密云记录不得自动公开。
* 用户应能在创建或编辑时清楚看到当前可见性状态。
* 用户应能随时将档案或记录改为“仅自己可见”。
* 如果档案设为私密，其下属记录和媒体也不应被公开展示。

状态对应关系：

| 状态 | 数据位置 | 是否免费 | 是否公开 |
| --- | --- | --- | --- |
| 临时本地使用 | 当前设备 App 私有存储 | 免费 | 否 |
| 注册后本地离线 | 当前设备 App 私有存储 | 免费 | 否 |
| 云空间私密 | Supabase 云端 | 收费 | 否 |
| 公开发现 | Supabase 云端 | 收费 | 是 |

开通云会员后的迁移规则：

* 已有本地档案同步为云空间私密。
* 已有本地记录同步为云空间私密。
* 已有本地图片同步为云空间私密。
* 旧内容不得自动公开。
* 用户可以选择以后新增记录默认私密或默认公开。
* 如果用户选择以后默认公开，必须明确提示：已有记录仍为私密。

公开内容限制：

* 不公开用户邮箱、手机号、支付信息、后台备注、系统 ID 等敏感信息。
* 不公开精确地址、家庭住址、门牌号、精确定位等敏感位置数据。
* 上传图片如包含 EXIF 位置信息，后续应考虑清理或避免公开。
* Discover 只能读取被标记为公开的档案或记录。
* 私密内容不能通过 URL 猜测、搜索、接口或导出泄露。

产品原则：

* 上传云空间不等于公开。
* 公开发现必须由用户主动选择。
* 旧记录不得自动公开。
* 公开 / 私密切换应简单明确。
* 不要把公开机制设计成强社交、流量竞争或排名机制。

求助规则：

* 求助不是独立论坛，而是记录的一种状态。
* 用户从某一条记录发起求助。
* 求助记录需要公开给其他用户查看。
* 私密记录点击求助时，必须提示：求助会公开这一条记录和相关图片。
* 用户确认后，才可以将该条记录设为公开求助。
* 用户的其他私密记录不得因此公开。
* V1 可以采用“项目壳公开 + 单条记录公开”的方式实现。
* 后续可支持“项目私密，但单条记录公开求助”的更细权限。
* 求助状态包括：无、求助中、已解决。
* 已解决记录应允许用户补充解决说明，沉淀为真实案例。

---

## 七、国际化与双语规则

本项目定位为全球用户可使用的有时·耕作 / LifeSpace for Cultivation。

规则：

* 不要只按中国大陆本地网站来设计。
* 产品至少需要支持中文 / 英文双语切换。
* 当前阶段可以先以中文为主要界面语言，但代码结构应为后续英文界面预留空间。
* 不要把大量中文文案直接硬编码在组件里。
* 后续如做 i18n，应逐步迁移到统一的文案配置或语言文件。
* 时间、日期、价格、单位、货币、地区表达要考虑国际化。
* 用户界面的普通日期统一显示为 `YYYY/MM/DD`；需要精确时间的详情、评论、通知和操作记录显示为 `YYYY/MM/DD HH:mm`，普通界面不显示秒。原生日期输入值、数据库值和机器读取字段仍按技术标准保存，不直接作为用户界面格式展示。
* 页面设计不要依赖中国大陆特有平台、手机号注册、微信生态或国内访问环境。
* 如果未来有注册，也要考虑国际用户邮箱注册。
* 不要使用过度本土化、网络流行语、强地域化表达。
* 产品气质应保持安静、自然、长期记录感，适合全球用户理解。
* 后续新增页面或组件时，应尽量把用户可见文案集中管理，方便翻译和双语切换。

---

## 八、主要模块

当前目标模块包括：

* 首页 / Landing Page
* 注册 / 登录
* 我的空间 / Dashboard
* 档案列表 / Archives
* 档案详情 / Archive Detail
* 时间线记录 / Records
* 媒体图片 / Media
* Discover / 发现页
* 云空间 / Pricing
* 导出 / Export
* 设置 / Settings

核心耕作模块包括：

* 作物档案
* 作物库
* 地块 / 花盆 / 种植床 / 小院 / 阳台
* 耕作记录
* 地图 / 平面图
* 本地气候
* 农法
* 设施制作
* 技术技能
* 自动化灌溉
* 传感器
* AI 辅助照料

轻扩展模块包括：

* 插花
* 写字
* 萌宠
* 健身
* 旅行
* 其他个人生活记录

轻扩展模块不应优先于耕作主线。

---

## 九、耕作内容范围规则

本项目当前阶段以“耕作 / 种植 / 养护”为主轴，不只是记录植物状态，也应逐步支持与耕作相关的农法、设施制作、技术技能和实践经验。

### 一、作物与种植记录

* 作物档案
* 播种、育苗、移栽、定植、修剪、采收记录
* 浇水、施肥、覆土、除草、病虫害处理记录
* 作物生长照片和时间线
* 作物库与基础养护参数

### 二、地块与空间管理

* 小院
* 阳台
* 花盆
* 种植床
* 温室
* 地块
* 水池
* 堆肥区
* 工具区
* 耕作地图 / 平面图

### 三、农法与种植方法

后续可支持与农法相关的内容记录和资料整理，例如：

* 自然农法
* 有机种植
* 覆盖种植
* 轮作
* 间作 / 混种
* 堆肥
* 绿肥
* 水培
* 基质栽培
* 育苗方法
* 土壤改良
* 病虫害生态防治
* 节气种植经验
* 地区化种植经验

### 四、设施制作与工具记录

后续可支持耕作设施和工具制作记录，例如：

* 育苗架
* 遮阳棚
* 防虫网
* 防雨棚
* 堆肥箱
* 花架
* 种植床
* 自动浇水装置
* 雨水收集
* 简易温室
* 水培设施
* 沼泽过滤
* 小型生态系统设施

设施内容可以作为独立档案，也可以关联到具体地块、作物或记录。

### 五、技术技能与实践知识

后续可支持用户记录和整理实践技能，例如：

* 嫁接
* 扦插
* 压条
* 分株
* 育苗
* 留种
* 修剪
* 授粉
* 采收
* 储藏
* 发酵
* 土壤判断
* 肥料制作
* 病虫害识别
* 设施维护
* 工具使用

这些内容不应做成权威百科，而应优先服务于用户自己的长期实践记录和经验积累。

---

## 十、耕作地图规则

未来“耕作 / 种植”模块应支持地图或平面图显示。

目标不是做复杂 GIS 系统，而是帮助用户以空间方式记录自己的种植区域、地块、花园、小院、阳台、温室或农场。

未来可支持：

* 显示用户自己的种植空间布局。
* 支持地块 / 分区 / 花盆 / 种植床 / 水池 / 温室等空间对象。
* 支持在地图或平面图上标记作物位置。
* 支持查看某一区域下的档案和时间线记录。
* 支持按区域查看种植历史、轮作记录、病虫害记录、采收记录。
* 支持未来和“作物库”联动，例如某个地块当前种了什么作物。
* 支持简单的平面图模式，优先满足小院、阳台、地块记录，不急于做复杂真实地图。

地图形式建议：

第一阶段优先考虑：

* 自定义平面图
* 地块示意图
* 用户手动绘制或摆放区域
* 简单网格 / 区块布局

后续再考虑：

* 接入真实地图
* 经纬度定位
* 农场级 GIS
* 区域气候和地理数据联动

隐私规则：

* 不默认公开用户真实住址。
* 不默认公开精确经纬度。
* 不公开家庭住址、门牌号、精确定位等敏感信息。
* 如果地图内容进入 Discover / 发现页，应默认隐藏精确位置。
* 可以公开“模糊地区”，例如城市、气候区、种植区类型。
* 用户必须主动选择，才可以公开更具体的位置。
* 上传图片如包含 EXIF 位置数据，后续应考虑清理或避免公开。

当前阶段规则：

* 地图显示属于未来功能，不作为当前阶段优先开发任务。
* 当前阶段只需在数据库和产品结构上预留可能性。
* 不要为了地图功能大规模重构现有档案、记录和媒体结构。
* 如果后续开发地图功能，必须作为独立任务进行。

---

## 十一、本地气候规则

本项目未来耕作模块应把“本地气候”作为重要参考层。

耕作记录不应只记录作物本身，还应逐步支持用户结合所在地气候、季节、天气和小环境进行长期判断。

未来可支持：

* 用户所在地城市 / 地区
* 气候区
* 四季变化
* 节气
* 月平均温度
* 最低温 / 最高温
* 霜冻期
* 高温期
* 梅雨期
* 台风 / 暴雨期
* 干旱期
* 降雨量
* 空气湿度
* 日照时长
* 风向 / 风力
* 小院、阳台、地块的小气候备注

本地气候应能与以下内容联动：

* 作物库
* 播种时间
* 移栽时间
* 采收时间
* 浇水建议
* 遮阳建议
* 防寒建议
* 防雨建议
* 病虫害风险
* 地块 / 花盆 / 种植床
* 耕作地图 / 平面图
* 自动灌溉计划
* AI 辅助照料建议

地区化种植建议：

* 宁波地区播种建议
* 江南地区梅雨期管理
* 夏季高温遮阳
* 冬季防寒
* 台风前设施加固
* 南方潮湿环境病害预防
* 北方霜冻地区播种调整
* 不同地区同一作物的种植时间差异

数据来源分阶段处理：

第一阶段：

* 用户手动填写城市 / 地区
* 用户手动记录天气和气候感受
* 系统提供简单地区备注

第二阶段：

* 接入天气 API
* 获取当地温度、湿度、降雨、风力、天气预报
* 根据天气生成提醒

第三阶段：

* 结合传感器数据
* 结合地块小气候
* 结合历史记录生成更个性化建议

隐私规则：

* 不默认公开用户精确位置。
* 不默认公开家庭住址、门牌号、经纬度。
* 公开内容可以显示模糊地区，例如城市、气候区、种植区。
* 用户必须主动选择，才可以公开更具体的位置。
* Discover / 发现页不应泄露用户精确位置。
* 地图、照片、天气记录都要避免意外暴露住址。

当前阶段规则：

* 本地气候属于重要长期方向，但当前阶段不需要马上接入复杂天气 API。
* 当前阶段可以先预留城市 / 地区字段、气候备注字段、地块小气候备注、作物库中的地区化备注、记录中的天气 / 温度 / 湿度字段。
* 不要为了本地气候功能大规模重构当前代码。

---

## 十二、自动化灌溉与 AI 照料规则

本项目未来可逐步支持自动化灌溉、传感器数据、AI 辅助判断和机器人照料植物。

这些能力属于长期方向，不是当前阶段必须立即实现的功能。

### 一、自动化灌溉方向

未来可支持：

* 自动浇水设备记录
* 灌溉计划
* 定时浇水
* 按土壤湿度触发浇水
* 按天气预报调整浇水建议
* 按作物类型生成浇水提醒
* 记录每次自动灌溉时间、水量、设备状态
* 灌溉设备异常提醒
* 地块 / 花盆 / 作物与灌溉设备绑定

自动灌溉数据应能关联到：

* 用户
* 档案
* 作物
* 花盆
* 种植床
* 地块
* 耕作地图 / 平面图
* 时间线记录

### 二、传感器方向

未来可支持接入：

* 土壤湿度
* 空气温度
* 空气湿度
* 光照强度
* 土壤温度
* 水位
* 水质
* pH
* EC
* 设备电量
* 设备在线状态

传感器数据应作为辅助记录，不应替代用户自己的观察和判断。

### 三、AI 辅助照料方向

未来可支持 AI 辅助：

* 判断植物状态
* 分析叶片异常
* 提醒浇水、施肥、修剪、采收
* 根据作物库和历史记录给出养护建议
* 根据天气和季节调整提醒
* 根据用户的地块和作物情况生成任务清单
* 总结某个作物或地块的生长过程
* 发现异常趋势，例如长期缺水、过湿、光照不足等

AI 建议必须是辅助建议，不能作为绝对判断。

涉及食用安全、药用价值、病虫害用药、儿童和宠物安全时，应使用谨慎表述，不得生成未经验证的高风险建议。

### 四、机器人照料方向

未来可探索 AI 机器人或自动化设备参与植物照料，例如：

* 自动巡视
* 自动拍照记录
* 自动识别植物状态
* 自动浇水
* 自动补光
* 自动通风
* 自动提醒人工处理
* 与耕作地图、作物档案、设施档案联动

机器人照料不应替代用户的核心主体性。

产品仍应强调：

* 用户自己的照料
* 用户自己的观察
* 用户自己的长期记录
* 自动化只是辅助工具

### 五、安全与权限规则

自动化设备、AI 和机器人相关功能必须注意安全：

* 设备密钥、API token、webhook secret 只能服务端保存。
* 不能把设备控制密钥暴露到前端。
* 外部设备写入记录必须校验用户权限。
* 设备只能访问自己绑定用户的数据。
* 自动浇水、补光、通风等控制动作应有安全限制。
* 高风险操作应需要用户确认或设置明确规则。
* 系统应记录自动化操作日志。
* 设备异常时应有提示，而不是静默失败。
* 不要让 AI 自动执行高风险操作，除非用户明确授权。

当前阶段规则：

* 当前阶段只需在产品结构和数据库设计上预留可能性。
* 不要现在为了自动化灌溉、AI 机器人或物联网设备大规模重构现有代码。
* 如果后续开发，应分阶段进行。

---

## 十三、可计算的植物生态模型

本项目未来不应只建立静态的“作物库”或“植物百科”。

长期目标是建立一个“可计算的植物生态模型”。

这个模型的目标是：

* 把作物、地块、气候、土壤、水分、光照、季节、农法、设施、传感器和用户记录连接起来。
* 让系统不只是展示资料，而是能辅助判断、计算、提醒和推荐。
* 为未来自动化灌溉、AI 辅助照料、机器人照料植物提供数据基础。

### 一、模型核心对象

未来模型可逐步包含：

* 作物 / 植物
* 品种
* 地块 / 花盆 / 种植床 / 阳台 / 小院
* 土壤 / 基质
* 光照
* 水分
* 温度
* 湿度
* 风
* 降雨
* 季节 / 节气
* 本地气候
* 病虫害
* 农法
* 设施
* 工具
* 自动化设备
* 传感器
* 用户时间线记录

### 二、作物参数不只是文字说明

作物库中的内容应尽量结构化、参数化，而不是只写成文章。

例如：

* 光照需求
* 水分需求
* 肥力需求
* 通风需求
* 耐寒性
* 耐热性
* 耐湿性
* 耐旱性
* 酸碱适应范围
* 生长周期
* 播种温度
* 发芽温度
* 适宜生长温度
* 播种月份
* 移栽月份
* 采收月份
* 病虫害风险
* 管理难度
* 适合盆栽程度
* 适合地栽程度
* 适合新手程度

这些参数应为未来计算和推荐服务。

### 三、生态关系

未来可逐步记录作物之间、作物与环境之间的关系，例如：

* 伴生 / 混种关系
* 不宜相邻种植
* 轮作关系
* 前茬 / 后茬
* 竞争关系
* 遮阴关系
* 引虫 / 驱虫关系
* 需肥差异
* 根系深浅
* 对土壤的影响
* 对湿度和通风的影响

这些关系应支持未来地块规划、混种建议和轮作提醒。

### 四、计算目标

未来系统可基于植物生态模型进行计算，例如：

* 某作物是否适合当前地区和季节
* 某作物是否适合当前地块
* 当前是否适合播种、移栽或采收
* 当前是否需要浇水、遮阳、防寒、防雨
* 当前病虫害风险是否升高
* 当前地块是否适合继续种同一类作物
* 某两个作物是否适合混种
* 某地块下一季适合种什么
* 自动灌溉计划是否需要根据天气调整
* AI 是否需要提醒用户观察异常

### 五、数据来源

模型数据可以分阶段建立：

第一阶段：

* 用户自己的记录
* 手动填写的作物参数
* 本地经验备注
* 简单作物库

第二阶段：

* 本地气候数据
* 天气 API
* 地块和小气候记录
* 作物生长历史

第三阶段：

* 传感器数据
* 自动灌溉数据
* AI 图像识别结果
* 用户长期反馈
* 区域化种植经验

设计原则：

* 不做静态百科堆砌。
* 不把 AI 生成内容直接当作权威知识。
* 优先结构化、参数化、可计算。
* 所有计算结果都应作为辅助建议，不应替代用户判断。
* 涉及食用安全、药用价值、病虫害用药、儿童和宠物安全时，必须谨慎。
* 模型应服务于用户自己的耕作实践，而不是做成普通农业资讯平台。

当前阶段规则：

* 可计算的植物生态模型属于长期方向。
* 当前阶段不要为了这个目标大规模重构现有代码。
* 当前阶段只需在作物库字段、地块关系、气候记录、设施记录、自动化接口等方面预留可能性。

---

## 十四、数据库与权限规则

数据库相关信息请优先参考 `docs/database.md`。

重要原则：

* 不要擅自修改数据库结构。
* 不要擅自创建、删除或重命名表。
* 如果确实需要数据库变更，必须先提出方案，再创建 migration 文件。
* 不要直接操作生产数据库。
* 不要提交真实用户数据。
* 不要提交真实邮箱、真实图片、真实付款数据。
* 不要提交 Supabase service role key。
* 不要提交 PayPal secret。
* 不要提交真实 `.env.local`、`.env.production` 或其他真实密钥文件。

权限底线：

* 用户只能读取、修改、删除自己有权限访问的数据。
* 用户只能导出自己的数据。
* Discover 只能读取公开内容。
* 管理员功能必须依赖可靠的数据库权限或 RPC，不得只靠前端判断。
* 云会员权限不能只靠前端判断。
* 导出功能不能只靠前端判断。
* Storage 图片和文件必须限制用户只能访问自己有权限访问的资源。
* 不要弱化 RLS policies。
* 不要绕过 RLS。
* 不要把 service role key 暴露到前端。

特别注意：

* `get_my_membership`
* `is_app_admin`
* 所有 `admin_*` RPC 函数
* `media` bucket
* `avatars` bucket

这些对象当前需要结合 Supabase 后台定义、RLS、RPC、Storage policy 继续核对。

---

## 十五、环境变量规则

* `.env.example` 可以提交。
* `.env.local`、`.env.production`、真实 `.env` 文件不能提交。
* `NEXT_PUBLIC_*` 变量可以用于前端，但仍不能写入真实敏感密钥。
* `SUPABASE_SERVICE_ROLE_KEY` 只能服务端使用，禁止进入前端代码。
* PayPal secret、支付宝密钥、数据库直连密码、服务端密钥只能服务端使用。

---

## 十六、UI 与样式规则

* 手机端和电脑端样式必须隔离。
* 不要为了修手机端而破坏电脑端。
* 不要为了修电脑端而破坏手机端。
* 移动端要紧凑、清楚、易操作。
* 不要使用过大的卡片、图片、字体和行距。
* 档案卡片、记录卡片应保持清晰、克制。
* 长标题、长记录可以省略显示，但不要破坏布局。
* 不要把界面做成电商、资讯流或强社交平台风格。
* 首页、记录页、档案页应保持安静、自然、长期记录感。
* 设计应支持多年内容沉淀后的回看、整理和阅读。
* UI 不应只服务即时发布，而应服务长期档案。

---

## 十七、云会员、云空间与支付规则

当前阶段不做自动续费订阅。

云会员开通目标是：

用户一次性支付年费后，网站自动或半自动开通一年云会员，不需要管理员直接手动改数据库，或只在异常订单中少量人工处理。

首发价格策略：

* 本地离线：免费。
* 基础云空间容量：1GB。
* 中国大陆固定首发价：人民币64元/年。
* 海外固定首发价：8美元/年。
* 人民币和美元为分区固定价格，不按实时汇率换算。
* 当前不做自动续费。

云会员包含：

* 云端私密空间。
* 多设备同步。
* 云端备份。
* 公开发现。
* 发布求助。
* 评论、回复、点赞、有帮助反馈和关注。
* 完整植物参数、养护指引、相关种植记录，以及未来经验卡的创建、检索、筛选、比较和生长线。
* 同时最多 30 条“发布中”的集市内容。
* 基础导出。

经验卡的传播规则：

* 经验卡第一版由有效云会员从同一个云端项目中选择至少3条原始记录生成，不设累计上限；只保存标题、封面选择和来源关系，不复制记录正文与照片。
* 所有来源记录按原始时间排序，第一条为起点、最后一条为结果，中间为过程；每条保留“查看原记录”链接。
* 作者主动公开的单张经验卡可由游客通过直接链接完整查看；公共经验卡库、站内搜索、筛选、聚合和多案例比较暂不开发。
* 发布前必须明确提示项目基础外壳、所选记录和照片将公开；其他未选择记录保持原来的可见性。
* 项目或任一来源记录改为私密、进入回收站或被删除时，经验卡必须立即停止公开。
* 经验卡不重复计算来源照片容量；后续平台生成的预览图和MP4属于衍生文件，不计入用户可见容量。
* MP4第一版由经验卡作者在当前设备的浏览器中生成，不经过服务器渲染，不上传云端，也不计入用户可见容量。
* 所有被选记录都必须进入视频；每条记录的图片由用户逐条选择，也可以选择不使用图片，没有选择图片时使用文字背景。原记录文字自动生成烧录字幕，长文字自动分段，时长随记录数量和文字长度延长。
* 经验卡详情中MP4生成与预览区域放在时间线之前；有图片的记录画面应优先展示图片，字幕只占画面底部较小区域。
* 输出为静音H.264竖屏MP4；生成完成后在页面中循环预览，可直接调用设备文件分享或保存后上传到外部视频平台。
* 手机端项目详情采用“档案／记录／经验卡／生长线”同级入口；生长线未实现时保留同级占位。个人空间项目页应提供“我的经验卡”入口。
* 当前不承诺无限服务器视频渲染；以后是否增加云端渲染、保存或合理用量限制，应根据真实使用和成本另行确定。

注意：

* 不要承诺永久低价。
* 不要承诺无限云空间。
* 不要承诺所有未来功能都包含在基础云空间内。
* 面向个人用户的付费身份统一称为“云会员”；“云空间”只表示云端存储、同步和容量，不代替会员身份。
* 对外用户状态统一为“游客／本地用户／云会员”；“本地免费”可以作为价格或功能说明，但不作为用户称呼。
* 团队经营层统一称为“商业会员”，其他候选名称不再作为正式方案名。
* 数据库表、RPC、内部历史字段中已有 membership 命名可以保留，但用户可见文案应逐步迁移。

### 一、海外支付规则

海外支付优先使用 PayPal 一次性支付，而不是 PayPal 自动订阅。

规则：

* 用户支付 US$8 / year。
* 支付成功后，通过服务端 webhook / API 验证支付结果。
* 验证通过后，自动为用户开通 1 年云会员。
* 不要只以前端支付成功页面作为云会员开通依据。
* PayPal secret 只能服务端使用，禁止暴露到前端。
* PayPal 支付功能必须作为独立任务开发。
* 上线前必须使用 sandbox / 测试环境验证完整流程。

### 二、国内支付规则

中国用户云会员首发价为 ¥64 / 年。

当前阶段不做自动续费。

国内支付优先只选择一种方式：支付宝。

#### 第一阶段：半自动开通

如果暂时没有支付宝官方支付接入条件，先做半自动开通：

* 网站生成云会员订单。
* 用户按订单金额通过支付宝付款。
* 用户上传支付宝付款截图。
* 后台提供“确认付款并开通云会员”按钮。
* 管理员只核对付款截图并点击确认，不直接手动修改数据库。
* 系统自动写入或更新 memberships / user_memberships 表，为用户开通 1 年云会员。

第一阶段目标是减少人工操作，而不是强行接入完整支付接口。

#### 第二阶段：支付宝一次性支付自动开通

后续如果具备支付宝商户 / 产品开通条件，再升级为支付宝一次性年费支付：

* 用户支付 ¥64 / 年。
* 支付宝服务端异步通知付款结果。
* 网站后端校验订单号、金额、币种、支付状态、支付宝交易号和回调签名。
* 验证通过后，系统自动开通 1 年云会员。
* 不做自动续费。

#### 暂不接入微信支付

当前阶段不同时接入微信支付，避免增加复杂度。

微信支付可作为后续备选方案，仅在支付宝方案不合适或用户需求明显时再评估。

### 三、支付安全规则

* 支付模块必须作为独立任务开发，不要混在普通页面修改里。
* 支付成功必须由服务端确认。
* 云空间状态必须以后端数据库记录为准。
* 不允许只靠前端按钮返回结果开通云会员。
* 必须校验订单号、用户 ID、金额、币种、支付状态、平台交易号、回调签名。
* 必须处理重复 webhook / 重复回调，避免重复开通或重复延长。
* 必须记录支付订单状态。
* 必须记录云会员开通来源和支付平台交易号。
* PayPal secret、支付宝密钥、微信支付密钥、Paddle 密钥等都只能服务端使用，禁止暴露到前端。

建议数据库中至少保留：

* orders：订单表
* payments：支付记录表
* memberships / user_memberships：云会员状态表

orders 至少记录：

* id
* user_id
* plan_id
* amount
* currency
* status
* payment_provider
* created_at
* paid_at
* expires_at

payments 至少记录：

* id
* order_id
* user_id
* provider
* provider_transaction_id
* amount
* currency
* status
* raw_event_id
* created_at

memberships 至少记录：

* user_id
* plan
* status
* starts_at
* expires_at
* source_order_id
* updated_at

### 四、大空间方案

* 大空间方案不公开展示。
* 需要更大空间的用户联系管理员。
* 管理员邮箱：[yoyomibaobao@gmail.com](mailto:yoyomibaobao@gmail.com)。

### 五、集市规则

#### 集市定位

集市只负责：

* 信息展示
* 推广
* 引流
* 联系意向方

实际交易在其他平台或线下完成。

LifeSpace 当前不提供：

* 站内订单
* 站内付款
* 发货
* 退款
* 交易完成确认
* 成交担保

#### 发布类型

集市内容包括：

* 出售
* 交换
* 转赠
* 求购

四种类型共用同一套发布名额。

#### 用户可见状态

用户侧主要状态只包括：

* 发布中
* 已结束

不要增加：

* 已成交
* 交易完成
* 待付款
* 已付款
* 已发货
* 已退款

管理员隐藏和违规下架属于后台管理状态，不属于交易状态。

#### 发布名额

有效云会员最多同时拥有30条“发布中”的集市内容。

计数规则：

* 出售、交换、转赠、求购统一计数。
* 只统计当前“发布中”的内容。
* 已结束不占发布名额。
* 已删除不占发布名额。
* 编辑现有发布不会重复占名额。
* 新发布和将已结束内容重新发布，都必须检查名额。
* 名额限制以后必须由服务端或数据库执行，不能只依赖前端按钮。

#### 发布时长与商业会员

* 云会员的个人供需信息每条最长发布 30 天，到期后可一键续期；续期从操作时重新计算 30 天，并重新校验有效会员状态和发布额度。
* 商业会员是未来面向个人经营者与组织的商业及协作方案，包含主账号的云会员基础权益。
* 商业会员拥有商家主页、长期产品目录、分类与批量管理、供应状态、试用／试种招募、站内咨询和团队项目共同管理能力，不能只实现为“无限期普通帖子”。
* 商业会员采用“团队空间＋成员账号”：每位成员用自己的个人账号登录，由所有者邀请并授权，不允许多人共用同一登录账号和密码。
* 商家主页、产品目录、专业发布、招募、咨询和转入团队的项目归团队空间所有；成员退出后内容仍保留，重要操作记录实际操作人。
* 首版成员角色可先采用“所有者／管理员／成员”，并逐项控制商家资料、产品、招募、咨询、数据与成员管理权限。
* 受邀成员获得的是团队管理权限，不自动获得个人云会员权益；成员若要使用个人云空间、公开个人项目或进行个人集市发布，仍需单独开通云会员。
* 商业会员的长期产品档案与试用／试种招募是两种不同内容；招募必须设置名额、报名截止日期、任务周期、地区和条件、记录要求、回报与结果提交方式。
* 只有报名时仍有效的云会员可以申请试用／试种；本地用户可以咨询，但不能报名。
* 发布人可根据报名者主动公开、或在本次报名中明确授权展示的种植经验选定约定名额；不得读取或披露报名者的私密项目、私密记录、联系方式或未授权数据。

#### 与云空间容量的区别

明确区分：

* 1GB限制云端数据和媒体占用。
* 30条限制同时处于“发布中”的集市内容。
* 已结束内容不占发布名额。
* 已结束内容只要数据和图片仍被保留，就继续占用云空间容量。

#### 扩容包

集市发布扩容包目前只属于未来设想：

* 暂不实施。
* 不属于当前会员权益。
* 不进入当前开发范围。
* 不写入当前价格体系。
* 待真实用户普遍达到基础30条上限后再评估。

---

## 十八、导出规则

导出功能应只导出当前用户自己的数据。

允许导出：

* 用户自己的档案
* 用户自己的记录
* 用户自己上传的图片 / 媒体
* 用户自己的基础资料
* 可阅读的 HTML 文件
* 可选 JSON 元数据

禁止导出：

* 其他用户数据
* 其他用户评论
* 其他用户图片
* 支付秘密信息
* 系统隐私数据
* 管理后台数据

导出原则：

* 用户数据应尽量可带走。
* 不要把用户锁死在平台里。
* HTML 应作为主要可读导出格式。
* JSON 可以作为备份元数据，但不要只导出机器可读文件。

---

## 十九、冷存储与长期保存规则

本项目应支持长期数据沉淀，未来需要考虑冷存储 / 归档存储能力。

冷存储不是当前阶段必须立即开发的功能，但应作为长期架构方向预留。

### 一、冷存储目标

冷存储的目标是：

* 降低多年历史图片和大文件的长期存储成本。
* 保护用户多年积累的耕作记录、图片、地图、气候数据、设施记录和档案。
* 让低频访问的历史资料仍然可以被找回、导出和恢复。
* 支持“经得起时间沉淀，越久越香醇”的长期产品气质。

### 二、适合未来冷存储的内容

未来可考虑进入冷存储或归档层的内容包括：

* 多年前的原始大图
* 长期不访问的历史媒体
* 已完成项目的旧记录图片
* 用户主动归档的档案
* 导出包备份
* 历史版本数据
* 低频访问的旧地图、旧附件和旧媒体

### 三、不应轻易冷存储的内容

以下内容不应在未确认前进入冷存储：

* 用户最近活跃项目的图片
* 当前档案封面图
* Discover / 公开展示正在使用的图片
* 用户经常访问的档案和记录
* 系统需要即时读取的缩略图
* 云空间状态、支付记录、权限数据等核心业务数据

### 四、用户体验原则

冷存储不能让用户感觉自己的资料被平台“偷偷拿走”或“打不开”。

未来如果启用冷存储，应满足：

* 用户能知道哪些内容已归档。
* 用户可以申请恢复或重新激活。
* 用户自己的数据仍应支持导出。
* 冷存储内容不应被视为删除。
* 恢复可能需要等待，但必须有明确提示。
* 不应因为云空间过期就立即删除用户历史资料。
* 不应在用户不知情的情况下静默丢失图片、附件或历史记录。

### 五、技术原则

未来冷存储设计应遵守：

* 热数据和冷数据分层。
* 原图和缩略图可分层处理。
* 缩略图可保留在热存储，原图可进入冷存储。
* 冷存储对象仍需保留 owner、archive、record、visibility 等权限关联。
* 冷存储恢复后仍必须经过权限校验。
* 私密内容进入冷存储后仍然必须私密。
* 公开内容进入冷存储后也不能绕过权限规则。
* 不要只保存无法理解的对象路径，应保留可追溯元数据。
* 不要把 public URL 作为长期不可迁移依赖，应优先保存 storage_path / object key 等可迁移路径。
* 如果未来接入对象存储分层、归档层或外部冷存储服务，必须作为独立任务设计。

### 六、与导出功能的关系

导出功能应优先保证用户自己的数据可带走。

如果某些媒体已进入冷存储：

* 导出时应提示需要恢复后再导出。
* 或提供异步导出任务。
* 不应静默漏掉冷存储文件。
* HTML 导出包应尽量保留记录文字、时间线、图片引用和缺失提示。
* JSON 元数据可记录冷存储对象状态、恢复状态和原始路径。

### 七、与云空间和容量的关系

冷存储不应被设计成惩罚用户的机制。

未来可以考虑：

* 本地离线或基础云空间保留一定热存储空间。
* 长期低频访问内容进入归档层。
* 大空间用户可获得更多热存储或更快恢复能力。
* 云空间过期后不应立即删除历史数据。
* 删除、归档、恢复、导出必须有清楚规则。

### 八、当前阶段规则

当前阶段不开发复杂冷存储功能。

当前阶段只需注意：

* 数据结构不要封死未来归档能力。
* media / records / archives 未来可增加归档状态。
* 不要把图片 URL 设计成无法迁移的永久依赖。
* 应逐步使用 storage_path，而不是只依赖 public URL。
* 不要在未确认前自动删除用户历史数据。
* 冷存储相关功能以后必须作为独立任务设计，不要混在普通页面修改、Storage policy 修改或支付功能中。

---

## 二十、基础统计与隐私规则

本项目可以收集基础使用统计，用于判断 APK 下载、App 首次打开、注册转化、云空间开通转化和本地功能使用趋势，但不得侵犯用户隐私。

统计目标：

* 统计 APK 下载量。
* 统计 App 首次打开量。
* 统计注册转化。
* 统计云空间开通转化。
* 统计本地功能使用趋势。

规则：

* 下载统计可以记录 APK 下载事件。
* App 首次打开时，如有网络，可以记录 `app_first_open`。
* 完全离线用户从未联网时，无法被统计，这是可接受限制。
* 如果用户离线使用后首次联网，可以补报匿名使用事件。
* 统计不应上传用户的记录正文、图片内容、精确地址、邮箱、手机号等敏感信息。
* 匿名统计应使用 `anonymous_id`。
* 登录用户可以附带 `user_id`。
* 本地记录数量、项目数量可以作为匿名聚合指标补报，但不得上传具体内容。
* 需要在隐私说明中说明会收集基础使用统计。

建议事件类型：

* `apk_download`
* `app_first_open`
* `app_open`
* `register`
* `cloud_space_opened`
* `local_project_created`
* `local_record_created`
* `local_data_bound_to_account`
* `local_data_synced_to_cloud`

---

## 二十一、内容安全规则

作物库、物种库、农法、设施、技术技能、AI 建议等内容不能宣称绝对医学、药用或食用安全。

涉及以下内容时必须谨慎：

* 有毒植物
* 药用价值
* 食用安全
* 儿童安全
* 宠物安全
* 病虫害用药
* 农药 / 杀虫剂 / 药剂
* AI 图像识别结果
* 自动化设备操作

规则：

* 不要把 AI 生成内容当作权威百科发布。
* 系统资料应允许标记来源、版本和是否已人工校对。
* 高风险建议必须保守表达。
* AI 建议只能作为辅助判断，不应替代用户自己的观察和专业判断。

---

## 二十二、家庭农场与小型生产者方向

核心定位：

帮助家庭农场和小型生产者记录真实生产过程，并把这些过程转化为可追溯、可展示、可推广的商业信用。

目标用户包括：

* 家庭农场
* 小型苗圃
* 果园和采摘园
* 花木、多肉和种苗经营者
* 社区农场
* 特色种植户
* 小型生态农场
* 农产品工作室

未来值得验证的能力：

### 一、生产者主页

* 农场或生产者简介
* 经营类别
* 公开项目
* 真实生产过程
* 联系入口
* 外部交易平台入口

### 二、批次与生产履历

* 品种
* 地块或区域
* 种植批次
* 生长记录
* 采收批次
* 出圃批次

### 三、二维码追溯与展示

* 生产者选择公开哪些记录
* 客户扫码查看真实生产时间线
* 不自动公开全部内部资料
* 不把普通展示页宣称为法定认证系统

### 四、记录转推广

* 从真实项目或批次选择记录和图片
* 生成集市推广内容
* 避免重复填写和重复上传
* 推广内容仍然指向真实生产档案

### 五、基础团队能力

未来可逐步支持：

* 所有者
* 管理人员
* 工作人员
* 查看者
* 保留创建、修改和完成记录的操作痕迹

### 六、简单生产协作

未来可逐步支持：

* 任务
* 负责人
* 日期
* 投入品记录
* 采收记录
* 基础报表

### 七、推广效果

可以统计：

* 页面访问
* 集市曝光
* 联系按钮点击
* 外部链接点击
* 二维码扫码次数

由于交易在站外完成，不统计或宣称真实成交量、成交率。

### 八、当前不优先进入的方向

当前不优先开发：

* 大型农场完整 ERP
* 多基地复杂管理
* 财务会计
* 工资和工时核算
* 完整采购与仓储
* 农机设备管理
* 站内供应链订单系统
* 复杂合规报表
* 重型物联网平台
* 大量企业定制接口

原因：

* 开发和维护成本高
* 客户定制差异大
* 实施与售后要求重
* 容易偏离现有产品优势
* 当前优势是连接真实记录、长期时间线、对外展示和推广引流

家庭农场方向只有在个人版稳定并出现真实持续使用者后，才进入正式开发。

---

## 二十三、开发规则

每次修改应遵守：

* 一次只做一个小任务。
* 不要重构无关文件。
* 不要改无关页面。
* 不要在未确认前大规模重命名。
* 不要在未确认前改变产品方向。
* 不要在未确认前改变数据库结构。
* 修改前先说明计划。
* 修改后列出改动文件。
* 修改后说明如何测试。
* 如果 build / lint 失败，要说明原因。
* 如果需要我提供 Supabase schema、RLS、RPC 或 Storage policy，要明确列出缺失项。
* 如果存在安全风险，要明确指出。
* 涉及支付、权限、导出、Storage、管理员功能时，必须特别谨慎。
* 涉及未来功能时，先写方案，不要直接大规模开发。

---

## 二十四、当前阶段优先级

当前阶段优先做：

1. 删除记录、项目和账户的安全删除流程。
2. 公开用户空间统计和隐私边界修复。
3. 云会员权限与集市30条发布限制。
4. 1GB云空间容量的可信核算和服务端约束。
5. 统一云会员人民币64元、海外8美元的首发价格和会员权益状态。
6. 支付订单、付款确认和权益生效流程。
7. 可恢复的数据导出与导入。
8. 小范围封闭测试。
9. 家庭农场与小型生产者方向的真实需求验证。
10. 后续再发展作物库、地图、本地气候、自动化、AI和植物生态模型。

暂时不要优先做：

* 大规模重构
* 强社交功能
* 复杂聊天区
* 大而全植物百科
* 自动续费订阅
* 生产数据库直接变更
* 复杂后台统计
* 复杂 IoT 平台
* AI 自动控制设备
* 机器人照料功能
* 大型农场完整 ERP
* 复杂家庭农场团队系统
* 站内交易和订单
* 集市扩容包
* 未经真实用户验证的家庭农场重型功能

## 2026-08-01 体验与导航补充

* 经验卡标题下的“项目名 · 系统名”分别承担导航：项目名打开来源项目记录详情；有系统指引时，系统名打开对应指引。记录时间线下方不再重复显示“查看原记录”。
* 手机个人空间的首个“项目档案”摘要行应整体可点击，进入我的项目页。
* 网页未登录时，集市页不重复显示全局导航中已有的登录和注册按钮；未登录首页优先在常见桌面视口内完整展示核心介绍和入口。
* 个人空间项目页的“我的经验卡”使用紧凑单行入口，不使用大面积说明卡；经验卡页面中的本地MP4预览需兼容原生 `loop` 不可靠的浏览器，在播放结束时主动回到开头继续播放。保存后的MP4文件是否循环由相册或外部平台决定。
* 经验卡详情页保持紧凑：视频生成区在最前，片头显示发布用户名并适当延长；记录日期与记录文字放在同一字幕区域，不单独悬浮在画面右上角。指引详情使用“概要与种植办法／经验卡／种植记录”三个同级页面。
* 项目详情展开档案时由“项目档案”文字承担打开入口，项目名只展示项目身份。关注列表中的项目名与系统名同一行，图文区域高度对齐。
* 发现页不得通过删减或强行压缩文字区来缩短卡片；应去除等高拉伸和固定空白，把更新时间、记录数、持续天数、评论数等轻量信息移入图片区域，正文信息保持完整并按内容自然高度排列。
* 未登录首页不使用大面积白色英雄卡框，主标题与口号字号保持克制；注册和登录只保留全局导航入口，首页主体不重复显示。

## 2026-08-08 账号页与经验卡补充

* “我的经验卡”打开页同时承担查看和编辑：作者无需再进入第二个编辑页面，可在同一页真实保存标题、至少3条且不设累计上限的来源记录和经验卡封面；旧编辑地址仅兼容跳转到统一详情页。
* 经验卡的云端封面与本机MP4选图必须明确区分。视频选图和视频片头只保存在当前设备，不得用“经验卡封面”文案误导用户；经验卡封面通过经验卡编辑保存到云端。
* 经验卡创建与修改共用同一套“记录＋缩略图”编辑界面：记录按原始时间完整列出，每条记录下直接显示所属图片；创建时默认选择全部记录和图片并自动确定封面，修改时保留原选择，同时允许加入项目后来新增的记录。选择记录时默认全选其图片，取消记录时同步取消其视频图片。
* 经验卡编辑器应提供“新增项目记录”入口；新增后直接刷新当前记录清单、自动加入经验卡并默认全选其图片。新增内容必须写入来源项目，经验卡仍只保存来源关系，不复制记录正文或照片。
* 公开经验卡详情应持续提供收藏、“有帮助”、评论和“写评论”入口；本人不能给自己的经验卡收藏或标记“有帮助”，但可以查看统计、管理评论和参与回复。私密草稿不显示不可用的公开互动输入框。
* 桌面全局导航不单独放置突兀的“云会员／云空间”购买入口；会员入口归入用户资料页的“会员与容量”模块，具体受限页面可使用“了解云会员”作为解释入口。
* “云空间”只在说明真实存储位置、容量或本地转云端时使用；导航、账号身份和泛权限提示不要反复使用“云空间”。
* 用户资料页在桌面端与手机端统一使用并列模块入口：用户信息／会员与容量／个人空间／回收站／数据与安全；管理员另有会员管理入口。
* “导出我的记录”属于用户的数据管理能力，放在“数据与安全”，不得放进会员权益或会员容量区块，也不得让用户误以为只有付费会员才能导出。
* 未登录首页的会员入口命名为“会员类别与权限”，并用简短说明直接区分免费使用、云端保存与公开互动；手机首页顶部与桌面导航均需显示“注册”，位置先于发现入口，登录入口保持可见。
