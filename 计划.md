# rc09-r4-converge 当前计划

工作区：`C:\Users\67049\Desktop\life-space\.worktrees\rc09-r4-converge`  
分支：`rc09-r4-converge`  
整理日期：2026-09-24

本文件只记录当前 worktree 的执行计划。产品规则以 `总纲.md` / `AGENTS.md` 为准。

## 当前开发策略（2026-09-24）

本阶段不是普通网页优化，而是 **local-first 离线统一**。

目标：在线页面保持不变；本地项目与云项目继续同一套 UI；完善云项目离线缓存；列表和详情明确区分「本地项目 / 云项目 / 云项目离线缓存」；离线时可以切换数据来源。

在当前 worktree 改、用网页和手机网页预览。不要为此阶段单独打 Android 包。

本阶段禁止：

* 生成 Android APK
* 触发 Android build workflow / GitHub Actions 打包装
* 修改 Capacitor、Android 在线入口、离线启动架构
* 做第二套离线 UI
* 把云缓存变成本地项目

一批 UI 和离线功能阶段性完成、并经用户确认后，再统一：

1. commit
2. push
3. 用与现有正式签名、包名一致的测试 APK 覆盖安装做真机验收
4. 真机验收通过后，再单独进入「本地项目可恢复存储」阶段（见第六节）

## 一、本轮目标

1. 恢复 rc9-r3 的 Android 在线体验：WebView 打开 `https://life-space.uk` 的完整 Next 移动端。
2. 保留 local-first 离线能力：断网或启动失败走 bundled `offline.html`。
3. 本地项目与云项目使用同一套卡片和详情结构；云项目视觉与功能保持原样。
4. 不把 `mobile-offline-src` 做成主 UI，不重写离线架构，不合并 r3 其他功能。

## 二、架构原则

在线：

```
Android → Capacitor WebView → https://life-space.uk → 正式 Next 移动端
```

离线：

```
Android → bundled offline.html → local-first shell → 本地项目 / 云缓存只读副本
```

必须遵守：

* 不 merge r3，不 cherry-pick，不 reset，不清除手机应用数据。
* 不新增独立本地页面，不新建第二套卡片组件。
* 不修改离线启动架构（除已完成的最小入口恢复）。
* 本地数据继续用现有 IndexedDB / local project；云数据继续用现有云端数据。
* 保持 IndexedDB origin 与登录态可用。
* 不删除云缓存、本地项目、pending sync。

## 三、已完成

1. 最小入口恢复（commit `cdb8644`，已随分支 push）
   * 恢复 `capacitor.config.ts` 的 `server.url`
   * `index.html` 为品牌占位；`offline.html` 仍是 local-first shell
2. 本地 / 云项目 UI 统一（`9413080`）及列表微调（`5ff87ce`）
   * 本地卡片 / 详情与云端手机结构对齐
   * 本地状态：本地项目 / 仅保存于此设备 / 转到云空间
   * 进行中项目排在已结束云项目之前；本地卡片不显示关注人数

## 四、当前任务

local-first 离线统一：业务代码已进入 Android 真机验收，确认前不提交。

补充规则已纳入本轮实现：

* 本地项目不受网络状态影响，在线 / 离线 / 云异常 / 未登录都可打开、浏览、编辑、新增记录并保存。
* 用户身份离线缓存：`user_id`、`username`、头像缩略图；在线更新、离线读取。
* 数据来源分开：本地 IndexedDB、云 Supabase、云缓存 IndexedDB `cloud-offline-cache` 只读。
* 标签：本地项目 / 仅保存于当前设备；云空间；云项目·离线缓存 / 只读查看。

本轮剩余：用与现有正式签名、包名一致的测试 APK 做覆盖安装真机验收。不卸载、不清数据。

核对：

1. 在线云项目列表 / 详情保持现有页面，不改成离线壳。
2. 本地项目继续用同一套卡片和详情。
3. 云项目离线缓存不得混进「本地」列表，也不得生成第二个可编辑项目。
4. 全部 / 云端 / 本地在离线时仍可切换，云端展示缓存只读副本。

## 五、暂不做

* 修改 Capacitor、Android 在线入口、离线启动架构
* 把 bundled shell 做成在线主界面
* 扩展 `mobile-offline-src` 页面去补发现 / 搜索 / 集市
* 迁移 r3 其他功能
* 改数据库结构
* 本轮实现「本地项目可恢复存储」（见第六节）

## 六、离线统一真机验收之后的独立阶段

### 本地项目可恢复存储

在当前离线统一 Android 真机验收完成后再单独开始。规则见 `总纲.md` 第二十六节。

本阶段才处理：

* 本地项目原始照片保存到用户可访问的公共目录（建议 `Pictures/LifeSpace/`）
* 拍摄直接写入该目录；相册导入复制一份，不依赖也不删除原图
* `cloud-offline-cache` 缩略图不进入永久照片目录
* 可恢复的项目元数据，以及“从本机 LifeSpace 资料恢复”
* 不使用 `Android/data/...` 作为长期可恢复资料目录
* 「数据与安全」说明照片位置、导入复制、卸载不删持久照片、恢复资料用途

进入该阶段前，不改当前离线统一代码、Android 存储实现、图片保存逻辑、Capacitor 或当前 APK 构建流程。
