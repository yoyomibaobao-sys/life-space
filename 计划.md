# rc09-r4-converge 当前计划

工作区：`C:\Users\67049\Desktop\life-space\.worktrees\rc09-r4-converge`  
分支：`rc09-r4-converge`  
整理日期：2026-09-24

本文件只记录当前 worktree 的执行计划。产品规则以 `总纲.md` / `AGENTS.md` 为准。

## 一、本轮目标

1. 恢复 rc9-r3 的 Android 在线体验：WebView 打开 `https://life-space.uk` 的完整 Next 移动端。
2. 保留 local-first 离线能力：断网或启动失败走 bundled `offline.html`。
3. 本地项目与云项目使用同一套卡片和详情结构；云项目视觉与功能保持原样。
4. 不把 `mobile-offline-src` 做成主 UI，不重写离线架构，不合并 r3 其他功能。

## 二、架构原则

在线：

```
Android → Capacitor WebView → https://life-space.uk → 正式 Next 移动端
```

离线：

```
Android → bundled offline.html → local-first shell → 本地项目 / 云缓存只读副本
```

必须遵守：

* 不 merge r3，不 cherry-pick，不 reset，不清除手机应用数据。
* 不新增独立本地页面，不新建第二套卡片组件。
* 不修改离线启动架构（除已完成的最小入口恢复）。
* 本地数据继续用现有 IndexedDB / local project；云数据继续用现有云端数据。
* 保持 IndexedDB origin 与登录态可用。
* 不删除云缓存、本地项目、pending sync。

## 三、已完成

1. 最小入口恢复（commit `91c2326`，未 push）
   * 恢复 `capacitor.config.ts` 的 `server.url`
   * `index.html` 为品牌占位；`offline.html` 仍是 local-first shell
   * 相关测试与 CI 检查已对齐
2. 本地 / 云项目展示对齐（代码已改，**未提交**）
   * 本地卡片复用 `ArchiveProjectCard` / `ProjectSummaryCard`
   * 本地详情复用 `ArchiveDetailHeaderView`
   * 本地状态：本地项目 / 仅保存于此设备 / 转到云空间

## 四、当前任务（未完成）

确认 UI 后再提交。不要提交未确认的展示改动，也不要提交 `总纲.md` / `计划.md`，除非用户明确要求。

核对项：

1. 云项目详情保持原样（本人页需登录后核对）。
2. 本地项目详情与云本人页结构一致。
3. 本地状态标识清晰。
4. 档案页卡片尺寸、图片、文字一致。

预览中已看到：

* 手机端本地卡已与云手机卡同一套组件；状态标识清楚。
* 桌面端仍不一致：云分组用原来的 `ArchiveCard`，本分组用 `ArchiveProjectCard` 桌面布局。
* 公开访客云详情是另一套访客页，不能代替本人云项目详情。

下一步（用户确认后执行）：

* 若要求桌面完全一致：让本分组桌面卡对齐现有云端 `ArchiveCard` 视觉，且不改云端卡片。
* 用户确认 UI 后，只提交展示对齐相关代码。
* 不 push，除非用户明确要求。

## 五、暂不做

* 把 bundled shell 做成在线主界面
* 扩展 `mobile-offline-src` 页面去补发现 / 搜索 / 集市
* 迁移 r3 其他功能
* 改数据库结构
* 覆盖安装正式签名 APK（debug 包不能覆盖 versionCode 19/21/22 的正式包）
* 真机在线 / 离线验收（入口恢复后另排）
